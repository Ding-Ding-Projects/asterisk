{
  "surface": "pages-site",
  "schemaVersion": 1,
  "readme": "Localization evidence for the Pages site, one row per canonical feature. The site localizes through its own COPY table rather than the console boundary, because it is a separate artifact that ships without the renderer. `copyKeys` names the entries a feature depends on; a row is `localized` only when every one of them exists in site/app.js, so this file is checkable against that source rather than asserted. The contract test recomputes every state and fails on any row that disagrees.",
  "languageModes": [
    "en",
    "zh",
    "both"
  ],
  "knownCopyKeys": [
    "changelogDesc",
    "dialogEmojisDesc",
    "displayNameDesc",
    "emptyDestinations",
    "emptyNotifications",
    "heroLede",
    "motionDesc",
    "narrationDesc",
    "notifRegexApplied",
    "notifSettingSaved",
    "notifSettingsReset",
    "schoolDesc",
    "searchModePlain",
    "searchModeRegex",
    "supportDesc",
    "supportFirstResponse",
    "themeDesc",
    "updatesDesc"
  ],
  "features": {
    "language-modes": {
      "state": "localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [
        "heroLede"
      ],
      "missingCopyKeys": [],
      "note": "Covered by COPY keys: heroLede."
    },
    "funny-levels": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "dialog-emojis": {
      "state": "localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [
        "dialogEmojisDesc"
      ],
      "missingCopyKeys": [],
      "note": "Covered by COPY keys: dialogEmojisDesc. The dialog-emoji card description carries four English and four Cantonese variants, and every one of the eight states both facts the switch turns on: that the wording is unchanged, and that no button, label or screen-reader name carries a glyph. The checkbox label, the card heading and the live status line are still fixed English."
    },
    "school-mode": {
      "state": "localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [
        "schoolDesc"
      ],
      "missingCopyKeys": [],
      "note": "The card ships COPY.schoolDesc with four English and four Cantonese funny levels, wired through data-copy=\"schoolDesc\". None of the eight variants names the mode, deliberately: the name is the reader's to change, so it is written into the heading, the status line and the search keywords at run time instead, and a description that named it could never be renamed with it. The status, suppression and recovery sentences are generated at run time from the chosen name and render in English, which is the only language available while the mode is on."
    },
    "narration": {
      "state": "localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [
        "narrationDesc"
      ],
      "missingCopyKeys": [],
      "note": "The narration card ships COPY.narrationDesc with four English and four Cantonese funny levels, wired through data-copy=\"narrationDesc\". Every level states both facts the card rests on: the narrator is off until it is switched on, and everything it reads is already on screen. The two voice-status lines and the spoken lines themselves are generated at runtime; the spoken lines carry their own en/zh wording at each call site, and are deliberately read from copyLevel() rather than copyText() so no personal-vocabulary replacement can be spoken through a network-backed voice."
    },
    "scheduled-settings": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "external-settings-sources": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "dim-sum-surprise": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "regex-builder": {
      "state": "localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [
        "notifRegexApplied",
        "searchModePlain",
        "searchModeRegex"
      ],
      "missingCopyKeys": [],
      "note": "Covered by COPY keys: notifRegexApplied, searchModePlain, searchModeRegex."
    },
    "non-blocking-notifications": {
      "state": "localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [
        "emptyNotifications",
        "notifSettingSaved",
        "notifSettingsReset"
      ],
      "missingCopyKeys": [],
      "note": "Covered by COPY keys: emptyNotifications, notifSettingSaved, notifSettingsReset."
    },
    "status-hub": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "material-appearance": {
      "state": "localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [
        "themeDesc"
      ],
      "missingCopyKeys": [],
      "note": "Covered by COPY keys: themeDesc."
    },
    "app-logo-customization": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "local-file-converter": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "ollama-suite-manager": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "browser-style-tabs": {
      "state": "localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [
        "emptyDestinations"
      ],
      "missingCopyKeys": [],
      "note": "Covered by COPY keys: emptyDestinations."
    },
    "tab-groups-and-searches": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "command-palette": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "destructive-action-confirmation": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "local-version-history": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "changelog-viewer": {
      "state": "localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [
        "changelogDesc"
      ],
      "missingCopyKeys": [],
      "note": "Covered by COPY keys: changelogDesc. The changelog description carries four English and four Cantonese variants, and every one of the eight states the two facts the viewer rests on: each line carries the real commit that made it, and what is exported is exactly what is on screen. The section heading, the control labels and the count, range and problem lines are still fixed English. The entries themselves are deliberately NOT localized and never will be: a change summary is a factual external record of what a release said, and restyling it at a funny level would stop it being a record."
    },
    "external-editor-handoff": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "complete-exports": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "bulk-actions": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "accessibility": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "responsive-sizing": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "personal-vocabulary-upload": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "per-element-toy-locks": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "support-tickets": {
      "state": "localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [
        "supportDesc",
        "supportFirstResponse"
      ],
      "missingCopyKeys": [],
      "note": "COPY.supportDesc ships four English and four Cantonese variants wired through the card's data-copy=\"supportDesc\" hook, and COPY.supportFirstResponse ships four of each as the canned first answer written into a new ticket's own update list, so a ticket keeps the voice it was answered in rather than being re-rendered later at whatever level is current. The joke is allowed to move with the slider; two things never do. SUPPORT_DISCLOSURE is a plain constant rendered verbatim into every .support-disclosure node -- not a COPY key, not passed through applyVocabularyText, and therefore reachable by no funny level, no language mode and no uploaded vocabulary file. SUPPORT_SEVERITY_NOTE is fixed for the same reason: it is the sentence admitting that severity changes nothing. The resolution steps, the storage-key list and the status labels are generated from the code's own constants and render in English, which is also the only language available while the restricted presentation is on."
    },
    "unlock-ladder": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "built-in-authenticator": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "attention-modes": {
      "state": "localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [
        "motionDesc"
      ],
      "missingCopyKeys": [],
      "note": "Covered by COPY keys: motionDesc."
    },
    "browser-extension-download-surfaces": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "offline-documentation-browser": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "app-display-name": {
      "state": "localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [
        "displayNameDesc"
      ],
      "missingCopyKeys": [],
      "note": "Covered by COPY keys: displayNameDesc. The display-name card description carries four English and four Cantonese variants, and every one of the eight names the shipped product so the identity boundary is stated at every funny level. The field label, the reset button and the live status line are still fixed English."
    },
    "guided-forms": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "bounded-overlays": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "context-menu-shortcuts": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "long-operation-progress": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "in-context-recovery": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "provider-markup-rendering": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "forge-publishing": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "collapsible-filters": {
      "state": "not-localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [],
      "missingCopyKeys": [],
      "note": "The site has a working language mechanism (COPY/copyText, both funny sliders, live vocabulary) but no COPY entry covering this feature, so its copy renders English in every mode. Missing translation, not missing wiring."
    },
    "automatic-updates": {
      "state": "localized",
      "mechanism": "console/site/app.js -- the COPY table (per-key en/zh variants at four funny levels), copyText() as the selector, and applyVocabularyText() over the result",
      "copyKeys": [
        "updatesDesc"
      ],
      "missingCopyKeys": [],
      "note": "COPY.updatesDesc ships four English and four Cantonese variants, wired to the sliders from the start through the settings card's data-copy=\"updatesDesc\" hook. Voice moves with the slider; four facts never do -- every one of the eight states that the comparison is on the build commit rather than the version label, that nothing installs or downloads in the background, that reloading is what takes the new page, and that the check asks this same site for one small file and sends nothing anywhere. The banner and status wording is rendered from the watch own state rather than from COPY, so it is English at every level; it is a factual report of two build identities rather than product prose, and every string in it still passes through applyVocabularyText()."
    }
  }
}
