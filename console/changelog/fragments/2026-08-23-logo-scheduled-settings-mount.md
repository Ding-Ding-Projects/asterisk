# Logo and scheduled-settings mount

- Added bounded local logo inspection, private converted-asset cache actions, desktop file-picker bridge, and renderer lifecycle wiring.
- Added validated HTTPS and Home Assistant source refresh actions with deadline, response bounds, redirect refusal, generation cancellation, in-memory fallback, and renderer-safe status projection.
- Recorded the unavailable decoder, absent OS-vault reader, unrun checks, and remaining UI and built-artifact work instead of claiming either source is fully shipped.
- Added the reachable settings route, hydrated settings and external-source subscriptions, strict DNS destination checks, the desktop OS-backed vault reader, and complete cache-envelope validation. The first repair kept conversion visibly disabled until the decoder worker was added in the follow-up repair.
- Added the declared `sharp` dependency, isolated decoder worker and packaged-resource entries. Conversion now performs internal-only cache writes after independent reopen validation, while the application-lifetime runtime applies effective settings and logo state to real document consumers.
- Added decoder health capabilities, locked-package revision and integrity checks, Windows Job Object memory enforcement, fixed-size cache reads, full-pixel reopen on activation, application-lifetime external cadence, per-rule cancellation, and two-key full-slider vault-reference removal confirmation.
