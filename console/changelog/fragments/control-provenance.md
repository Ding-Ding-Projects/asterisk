## Control provenance

- Added exact per-control configuration resource, section, key, parser, host writer capability, state, observation timing, freshness, and disabled-reason contracts.
- Added structured resource arrays for multi-file screens and fail-closed host capability lookup.
- Added destination-specific table state with partial command aggregation, verified-empty results, accessible unread markers, complete live collection counts, and dialplan canvas subset metadata.
- Expanded the hand-written base-control inventory so every control declared by the compiled base screens is represented even when it is intentionally unmapped.
