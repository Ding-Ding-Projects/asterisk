{
  "schemaVersion": 1,
  "sourceArchive": {
    "sha256": "9A4284745A745C18A18B0A23D2A2F5851A79F9B6EFCBC5EE30EDCD69CEA2863F",
    "verification": "independent-authoritative-audit"
  },
  "auditBaseline": {
    "destinationCount": 32,
    "railCounts": {"pbx":8,"media":4,"data":2,"system":4,"agent":7,"app":7},
    "declarativeBindings": {
      "total": 265,
      "click": 212,
      "change": 10,
      "input": 10,
      "contextmenu": 9,
      "dragstart": 4,
      "dragover": 4,
      "drop": 4,
      "dragend": 4,
      "mousedown": 5,
      "mouseenter": 1,
      "mouseleave": 1,
      "mouseup": 1
    },
    "distinctExpressionCount": 168,
    "controlCount": 479,
    "transientStateFamilyCount": 17
  },
  "evidenceTemplates": {
    "referenceRoute": "console/design-reference/index.html?destination={id}&state=default&theme=dark&width=1440&height=1000&scale=1",
    "builtRoute": "ding-pbx://destination/{id}?state=default&theme=dark&width=1440&height=1000&scale=1",
    "referenceCapture": "console/release/captures/parity/{id}-reference.png",
    "builtCapture": "console/release/captures/parity/{id}-built.png",
    "sideBySide": "console/release/captures/parity/{id}-comparison.png",
    "visualDiff": "console/release/evidence/parity/{id}-diff.json",
    "regionLedger": "console/release/evidence/parity/{id}-regions.json",
    "chromeParity": "console/release/evidence/parity/{id}-chrome.json",
    "materialAudit": "console/release/evidence/parity/{id}-material.json"
  },
  "chromeParityBar": {
    "what": "The reference-versus-built bar a destination in this project can actually meet: outside the regions that carry data, the two artifacts must render identically. Whole-frame pixel identity is unreachable here by deliberate product decision — the design shows invented sample content exactly where the application shows the target's real, usually empty, readings — so 47% to 64% of every frame differs for a reason nobody wants fixed. This bar asks the narrower question instead.",
    "tolerance": 0,
    "whyToleranceIsZero": "Both sides are Chromium at the same device metrics reading the same local font files, so identical content really does produce identical pixels. The committed captures prove that rather than assert it: dash and logger each contain runs of rows that are byte-for-byte equal between the two sides. A non-zero tolerance here would be a number chosen until something passed.",
    "minimumComparedFraction": 0.25,
    "whyThereIsAMinimum": "A mask is the one way this bar could be passed without matching anything: widen the declared data regions until nothing is left to compare. The regions declared below leave a little under 30% of a 1440x1000 frame, so a floor of 25% sits below what an honest mask costs and far above what a mask widened to force a pass would leave. compareChrome refuses rather than compares when the floor is breached.",
    "howRegionsAreObtained": "Measured off both live DOMs by console/scripts/design-parity-regions.mjs during a capture run — never hand-drawn, and never derived from the pixel diff. A mask derived from the diff would exclude exactly what differs and turn every verdict into a match. The shell is located structurally (a three-row layout whose last row is three columns) because the two sides share the design's layout but not its class names, which both toolchains hash at build time.",
    "howExclusionsCombine": "The exclusion for an area is the union of the two sides' measured rectangles, not their intersection: the sides genuinely disagree about some heights, and an intersection would leave a strip of one side's data inside the compared region and report it as a chrome defect it is not. A union can only ever hide more, which is what the minimum compared fraction and the recorded excluded-region diff are there to keep honest.",
    "areaRoleJudgement": "The roles below are the one human judgement this bar rests on. They are declared once for the whole application rather than as 32 per-destination masks, so the judgement stays small enough to review and hard to widen quietly.",
    "areas": {
      "brandCell": {
        "role": "chrome",
        "why": "The top strip's first cell: the product mark and name. It renders the same thing on every destination and reads nothing from the target."
      },
      "menuCell": {
        "role": "chrome",
        "why": "The top strip's menu bar — a fixed set of menu titles, identical on every destination."
      },
      "commandCell": {
        "role": "chrome",
        "why": "The top strip's command affordance. It carries its own label, not a reading."
      },
      "statusCell": {
        "role": "data",
        "why": "The top strip's last cell carries live status: what the console is connected to and how that connection is faring. The design invents a healthy-looking value there; the built application shows whatever the target actually reports, which with no target configured is nothing at all."
      },
      "tabStrip": {
        "role": "chrome",
        "why": "The open-tab strip. Tab titles come from the navigation catalogue, which is itself compiled from the design, so both sides are naming the same screens."
      },
      "rail": {
        "role": "chrome",
        "why": "The navigation rail: six fixed rail icons and their labels, compiled from the design's own catalogue. Chrome deliberately — a divergence here is a real layout defect and the bar is supposed to report it rather than excuse it."
      },
      "sectionList": {
        "role": "chrome",
        "why": "The section list for the open rail. Kept inside the compared region on purpose even though this application removed the design's per-row badges: the labels are chrome, and a badge present on one side and absent on the other is a divergence worth reading in the result rather than one worth hiding in a mask."
      },
      "contentPane": {
        "role": "data",
        "why": "The destination's own screen. This is where the design's invented rows, dashboard tiles, health bars and history sit, and where the built application puts the target's real readings instead. It is the region this bar exists to exclude."
      }
    }
  },
  "compiledEvidence": {
    "method": "compile-then-render: console/scripts/compile-design.mjs compiles design/Asterisk Console M3.dc.html and design/M3 Control.dc.html into app/renderer/src/generated/{console.tsx,m3-control.tsx,design-styles.css,design-manifest.json}, which console/app/renderer/src/App.tsx subclasses directly rather than a hand-matched reimplementation.",
    "test": "console/tests/ui/design-parity.test.tsx",
    "coverage": "Renders all 32 destinations from the generated design model and asserts each one's design title, description, owning source file (Expert mode), its control groups (including the design M3 control's slider/stepper/order affordances), Beginner-mode plain wording, and all 17 transient-state overlay families."
  },
  "captureContract": {
    "status": "captures, diffs, region ledgers and chrome-parity records now exist for every audited destination bar About and are committed; all 32 rows nevertheless remain compiled, none verified — see chromeParityRun for what the new bar measured and what still stands between these rows and verified",
    "harness": "console/design-reference/index.html",
    "harnessDriver": "console/design-reference/route.mjs",
    "destinationLabels": "console/design-reference/destination-labels.generated.json",
    "destinationLabelsGenerator": "npx tsx console/scripts/generate-design-parity-labels.mjs [--check]",
    "captureManifest": "console/design-reference/capture-manifest.generated.json",
    "captureManifestGenerator": "node console/scripts/generate-design-parity-capture-manifest.mjs [--check]",
    "captureTuple": {"state": "default", "theme": "dark", "width": 1440, "height": 1000, "scale": 1},
    "diffTool": "console/scripts/design-parity-diff.mjs (compareCaptures)",
    "evidenceGuard": "console/scripts/design-parity-evidence-on-disk.mjs (verifyDesignParityEvidence)",
    "evidenceGuardNegativeProof": "console/scripts/negative-design-parity-evidence.mjs",
    "capturesGuard": "console/scripts/design-parity-captures-on-disk.mjs (verifyCapturedParityEvidence)",
    "capturesGuardNegativeProof": "console/scripts/negative-design-parity-captures.mjs",
    "captureRunner": "node console/scripts/design-parity-capture-run.mjs --side=reference|built|diff|chrome [--port=N] [--server-port=N] [--only=id,id] [--regions-only]",
    "captureHost": "console/scripts/design-parity-server.mjs",
    "chromeParityTool": "console/scripts/design-parity-chrome.mjs (compareChrome)",
    "regionRecorder": "console/scripts/design-parity-regions.mjs (regionProbeExpression, buildRegionLedger, maskFromLedger)",
    "chromeParityTests": "console/tests/scripts/design-parity-chrome.test.mjs",
    "chromeParityDocumentation": "console/docs/evidence/design-parity-chrome-bar.md",
    "closedGap": "design/support.js loads React itself (loadReactUmd) from unpkg, and reads window.__resources[url] first (cdnScriptFor), so the design already ships the host hook. console/scripts/vendor-design-react-host.mjs vendors react/react-dom 18.3.1 into console/design-reference/vendor/, verified against the sha384 subresource-integrity hashes design/support.js itself declares, and the capture host sets that map. Nothing under design/ is edited and a capture run makes no network request.",
    "builtRouteStatus": "ding-pbx:// is a committed route template only; no custom protocol handler is registered in console/app/ yet. The built side is captured the way every other Windows UI evidence in this project is: the real built renderer under Electron on an off-screen Windows desktop, driven to each destination through the same rail-then-section clicks the reference side performs.",
    "captureRun": {
      "ledgers": [
        "console/release/evidence/parity/run-reference.json",
        "console/release/evidence/parity/run-built.json",
        "console/release/evidence/parity/run-diff.json"
      ],
      "referenceCaptures": 32,
      "builtCaptures": 31,
      "diffRecords": 31,
      "verdicts": {"match": 0, "diff": 31, "refused": 0, "skipped": 1},
      "diffPercentageRange": "47.13% to 63.95% of pixels differ",
      "networkRequestsReachingTheNetwork": 0,
      "firstRunSurface": "both sides dismiss the product's own first-run wizard before capturing, because every audited destination sits behind it; the reference harness records that it did so in window.__captureDismissedFirstRun.",
      "areTheThirtyOneBuiltCapturesStale": "No, and the question is worth answering rather than leaving to inference, because the commit that removed the About heading divergence edited App.tsx's renderVals, which every destination renders through. The edit is confined to the 'about' screen: for every other screen the composed subheading is character-for-character what it was, and the removed screenTitle override only ever fired on 'about'. So the thirty-one committed built captures still show what this commit's renderer draws. compareCaptures' own staleness check compares file mtimes, which a fresh clone does not preserve, so it would not have caught this either way.",
      "notCaptured": {
        "about": "STILL ABSENT, BUT NO LONGER FOR THE RECORDED REASON. The run ledger's reason (the built <h1> reading 'About Ding PBX Console' where the design's reads 'About') was a real divergence in the application and has been removed: the heading is now the design's own 'About', and the console's name moved into the About screen's body, where the rename still reaches it. Two tests read the <h1> directly rather than off the flattened screen text, so a name moved back into the heading turns them red. What has NOT happened is a capture: the approved off-screen capture route was unavailable to the run that made this change, so about-built.png and about-diff.json are genuinely absent rather than quietly regenerated. The next --side=built run should settle on this destination like the other thirty-one; until one has actually done so, this stays an expectation and not a result."
      },
      "whyNothingWasVerifiedUnderTheOldBar": "verifyDesignParityEvidence requires a 'match' verdict, and compareCaptures calls a match only when the two captures are pixel-identical. That bar is unreachable here BY DELIBERATE PRODUCT DECISION, not by a defect in the harness: this project removed the design's sample rows, dashboard tiles, health bars, nav badges, history, agent rail and trunk-authentication content from the running application, so the reference shows invented content where the built application shows the target's real (often empty) readings. The diffs measure that divergence rather than a rendering fault — none of the 31 was refused as unpainted or stale, and the side-by-side images show identical chrome beside different data. Moving a row to 'verified' therefore needs a parity bar that compares chrome and layout with the data-bearing regions excluded, plus a real Material Design 3 conformance audit per destination; neither existed when this was written. SUPERSEDED IN FULL: the parity bar half now exists — see chromeParityRun — and so does the Material Design 3 half, see materialAuditRun. The objection that a generated audit nobody performed would be an invented verdict still stands, and is what shaped the auditor rather than what prevented it: its conforms value is computed from what it measured and cannot be supplied by a caller, so it can only ever report a divergence it actually found. It reports all 32 destinations nonconforming."
    },
    "chromeParityRun": {
      "whatChanged": "verifyDesignParityEvidence no longer asks the whole-frame visualDiff for a 'match'. A verified row now rests on the chrome-parity bar declared in chromeParityBar above: outside the regions measured as data-bearing, the two artifacts must render identically at tolerance 0. The whole-frame diff is still required and still read — it is the record of how far the data diverges — but it is now required to be a real comparison rather than a match, and a 'refused' one is refused exactly as before.",
      "ledgers": [
        "console/release/evidence/parity/regions-reference.json",
        "console/release/evidence/parity/regions-built.json",
        "console/release/evidence/parity/run-regions-reference.json",
        "console/release/evidence/parity/run-regions-built.json",
        "console/release/evidence/parity/run-chrome.json"
      ],
      "howTheRegionsWereMeasured": "Two --regions-only runs, which drive each side to every destination and measure its area rectangles without photographing anything. That mode exists so the bar could be applied to captures that were already committed: re-photographing them to obtain a mask would have replaced the very evidence being measured, and a mask taken from one render against pixels from another is exactly the staleness this harness refuses everywhere else. The reference side ran against the design export under headless Edge with the same request interception as a capture run; the built side ran against the same built renderer under Electron on an off-screen Windows desktop that the committed built captures came from.",
      "measured": 31,
      "notMeasured": {
        "about": "Still absent, and for the same reason as its capture: the built renderer available to this run reports its About heading as 'About Ding PBX Console', so the settle condition that proves a driver arrived cannot be satisfied. The heading divergence has been repaired in source; the build this run drove predates that repair. about-regions.json and about-chrome.json are therefore genuinely absent rather than quietly generated, and the next --side=built run from a current build should settle on it like the other thirty-one. That remains an expectation, not a result."
      },
      "verdicts": {"match": 0, "diff": 31, "refused": 0, "skipped": 1},
      "chromeDiffRange": "6.67% to 26.78% of the compared region differs",
      "comparedFractionRange": "29.5% to 29.6% of the frame is compared, against a declared floor of 25%",
      "worstAreaTally": {"commandCell": 30, "sectionList": 1},
      "whyNothingIsVerifiedYet": "Two things, and neither is a defect in the bar. FIRST, no destination meets it: every one of the 31 reports a real chrome divergence, so the bar is doing its job rather than waving them through. SECOND, a verified row still needs the per-destination Material Design 3 conformance audit. SUPERSEDED IN PART: that audit has now been performed and committed for all 32 destinations — see materialAuditRun below — and it reports every one of them nonconforming. It is therefore now a second measured reason nothing is verified, rather than an absent prerequisite.",
      "theOneDivergenceUnderneathMostOfIt": "brandCell differs by exactly 15.6% and menuCell by exactly 12.0% on EVERY one of the 31 destinations. Identical figures across 31 screens is the signature of one divergence rather than 31, and the region ledgers name it: the built shell is exactly 1440x1000 on all 31, while the reference shell is 1428 wide on 20 of the 32 (a 12px vertical scrollbar) and ranges in height from 622px to 7668px. The design export, rendered by its own runtime, lets the document grow to its content and scrolls the page; the application constrains its shell to the viewport and scrolls within panes. Every horizontal position in the top strip drifts as a result. Whether that is repaired in the application, in the design, or in the capture harness is a judgement for a later pass; what this bar did was turn '57% of pixels differ' into one named, measurable cause.",
      "supersededBy": "materialAuditRun, for the Material Design 3 half of whyNothingIsVerifiedYet only. Nothing else in this section changed.",
      "whatCouldStillBeWrongWithThisEvidence": "The region measurements and the committed captures come from the same built renderer and the same design export, but not from the same instant: the captures were taken by an earlier run and the rectangles by this one. compareChrome's staleness check ran and passed for all 31, and in THIS run it did not mean much: the run was made from a fresh linked worktree, and a checkout stamps every file with the time it was written, so what actually got compared was 'the checkout happened after the build' — which was never in doubt. That is the same limitation already recorded above for compareCaptures. Treat the 31 passes as an absence of contrary evidence rather than proof the captures postdate the build. The commit console/dist was compiled from is also not identified: it predates the About-heading repair, which is why About still refuses. A full single-pass run (--side=reference then --side=built without --regions-only, which measure the rectangles while the screen is settled and before anything else touches it) removes that gap and should be preferred when the captures are being retaken anyway."
    },
    "materialAuditRun": {
      "whatChanged": "The per-destination Material Design 3 conformance audit the verified guard requires has been performed and committed for all 32 audited destinations. Every prior note in this file saying no materialAudit file had been generated is superseded by this section; the reason those notes gave was right and still is, which is why the audit is a measurement rather than a written verdict.",
      "whyAMachineMayWriteThisOneAfterAll": "The objection recorded for several passes — that a generated audit nobody performed would be an invented verdict — is aimed at exactly one construction: a script that writes conforms:true without looking. auditMaterial is the opposite construction. `conforms` is computed as `defects.length === 0` from findings derived out of the rendered markup, there is no argument, option or code path that can set it, and a caller passing conforms:true is ignored — negative-design-parity-material.mjs plants that exact attempt and requires it to be refused. Every check here can only ever ADD a defect, so a check that fails to fire understates the divergence and none can manufacture a conformance. The direction of error is the point.",
      "auditor": "console/scripts/design-parity-material.mjs (auditMaterial, findStaleRecords)",
      "runner": "npx tsx console/scripts/audit-design-parity-material.mjs [--check] [--only=id,id]",
      "tests": "console/tests/scripts/design-parity-material.test.mjs",
      "negativeProof": "console/scripts/negative-design-parity-material.mjs",
      "documentation": "console/docs/evidence/design-parity-material-audit.md",
      "records": "console/release/evidence/parity/{id}-material.json, plus the run ledger console/release/evidence/parity/run-material.json",
      "whatWasAudited": "The built product renderer — console/app/renderer/src/App.tsx, the class the application actually mounts — rendered at each destination, together with the effective stylesheet the built renderer loads (app/renderer/src/styles.css and the generated design-styles.css it imports). Not the bare compiled shell App subclasses, and not the design export: the conformance question a verified row asks is about the chrome a person operates.",
      "checks": {
        "typeScale": "every explicit font-size must be one of the 15 Material Design 3 type-scale sizes",
        "iconSize": "every icon glyph must be drawn at one of the 4 Material Design 3 icon sizes",
        "shapeScale": "every corner radius must be one of the 6 shape-scale steps, or fully rounded",
        "elevation": "a shadow must be a two-layer Material Design 3 elevation, not a single custom layer",
        "stateLayer": "hover and pressed must be a translucent state layer over the existing surface rather than an opaque colour swap, and a focus state must exist",
        "touchTarget": "an interactive element must reach the 48dp minimum in both axes",
        "motion": "durations and easing curves must be Material Design 3 motion tokens"
      },
      "audited": 32,
      "conforming": 0,
      "nonConforming": 32,
      "elementsAuditedRange": "180 to 1433 elements per destination",
      "interactionRulesAudited": 57,
      "divergencesPerCheck": {"typeScale": 1955, "iconSize": 2364, "shapeScale": 578, "elevation": 33, "stateLayer": 1120, "touchTarget": 1274, "motion": 955},
      "divergencesPerDestinationRange": "155 to 970",
      "checksThatFoundNothingOnAnyDestination": [],
      "theShapeOfWhatItFound": "The design is a design-tool export whose measurements were chosen by eye rather than taken from the scale, and the audit says so in exact numbers rather than in adjectives. Type sizes land on 11.5, 12.5, 13, 13.5, 14.5, 15, 15.5, 18, 20 and 54px, none of which the type scale contains; icon glyphs on 14 through 22, 30, 32 and 34px against a scale of 20/24/40/48; radii on 5, 7, 9, 10, 14, 18, 20, 24 and 26px against 0/4/8/12/16/28. Every interaction state is an opaque colour swap rather than a state layer. Every shadow is a single custom layer rather than a two-layer elevation. Durations sit at 120, 160, 180, 220, 240, 280, 320, 340 and 360ms against a token set that has 100, 150, 200, 250, 300 and 350.",
      "whatItFoundConforming": "Not nothing, and this matters because an auditor that reported everything as wrong would be exactly as useless as one that reported everything as right. cubic-bezier(.2,0,0,1) — the curve most of this interface transitions on — IS the Material Design 3 standard easing token and is not reported; the two curves that are reported are overshoot springs. Icon glyphs at 20px and 24px pass. Radii at 8, 12, 16 and 999px pass. The application's own stylesheet declares focus states, so the focus half of the state-layer check does not fire. The auditor's tests additionally hold it against a fully conformant synthetic screen and require a clean verdict.",
      "whatThisAuditDoesNotClaim": "It reads declarations, not pixels. It cannot decide whether a control whose measurements are correct is a real Material Design 3 component or a custom lookalike sharing its numbers; it cannot see geometry that only exists after layout, so a flex child that declares no size is not held to the touch-target minimum; it cannot see a style injected at runtime; it cannot watch motion actually run; and it measures no colour tokens at all, so nothing here says whether a hard-coded colour is the colour role its position calls for. Every record repeats these five limits in its own notMeasured field so the limit travels with the verdict. Closing them needs the same driven-build route the captures use.",
      "freshness": "`--check` re-derives all 32 records from the current renderer and fails if a committed one would differ or is missing. It runs in npm run test:inventories, so a renderer change that moves a screen without re-auditing it turns the suite red rather than leaving a verdict about a screen nobody can reach."
    }
  },
  "destinations": [
    {"rail":"pbx","id":"dash","status":"compiled"},{"rail":"pbx","id":"live","status":"compiled"},{"rail":"pbx","id":"endpoints","status":"compiled"},{"rail":"pbx","id":"trunks","status":"compiled"},{"rail":"pbx","id":"trunkauth","status":"compiled"},{"rail":"pbx","id":"canvas","status":"compiled"},{"rail":"pbx","id":"ivr","status":"compiled"},{"rail":"pbx","id":"queues","status":"compiled"},
    {"rail":"media","id":"voicemail","status":"compiled"},{"rail":"media","id":"confbridge","status":"compiled"},{"rail":"media","id":"moh","status":"compiled"},{"rail":"media","id":"codecs","status":"compiled"},
    {"rail":"data","id":"cdr","status":"compiled"},{"rail":"data","id":"ami","status":"compiled"},
    {"rail":"system","id":"modules","status":"compiled"},{"rail":"system","id":"logger","status":"compiled"},{"rail":"system","id":"security","status":"compiled"},{"rail":"system","id":"cli","status":"compiled"},
    {"rail":"agent","id":"memory","status":"compiled"},{"rail":"agent","id":"sync","status":"compiled"},{"rail":"agent","id":"skills","status":"compiled"},{"rail":"agent","id":"hub","status":"compiled"},{"rail":"agent","id":"vocab","status":"compiled"},{"rail":"agent","id":"ops","status":"compiled"},{"rail":"agent","id":"secrets","status":"compiled"},
    {"rail":"app","id":"servers","status":"compiled"},{"rail":"app","id":"arcade","status":"compiled"},{"rail":"app","id":"notifications","status":"compiled"},{"rail":"app","id":"history","status":"compiled"},{"rail":"app","id":"customise","status":"compiled"},{"rail":"app","id":"appearance","status":"compiled"},{"rail":"app","id":"about","status":"compiled"}
  ],
  "transientStateFamilies": [
    "appearOpen","ceremonyOpen","ctxOpen","infoOpen","lockOpen","onboardOpen","paletteOpen","regexOpen","renameOpen","subOpen","sureOpen","tabColourOpen","tabFilterOpen","toastOpen","tourOpen","unlockOpen","wizardOpen"
  ]
}
