# Dim-sum and rich-control mount

- Mounted the local dim-sum startup surprise through a typed cache bridge and live startup suppression context. A missing private cache stays unavailable, with no invented dish or opt-out.
- Registered writable design settings and every appearance property with the shared command and palette registry, live value readers, exact targets, and the persisted appearance runtime.
- Verification remains unrun in this lane: no tests, lint, type check, build, package, runtime interaction, or capture was performed.
