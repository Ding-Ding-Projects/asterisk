# Ding PBX delivery roadmap

## Delivery foundation

- [x] Add a pinned, digest-verified, user-scoped Windows dependency bootstrap.
- [x] Add touchless and interactive root build entry points.
- [x] Add unsigned Squirrel.Windows package verification for `Setup.exe`, `RELEASES`, and full packages.
- [x] Replace inherited test and administration workflows with separate build/package/release and Pages publication workflows.
- [x] Add unique release tags, workflow timing, SHA-256 records, safe failure artifacts, and runner fallback.
- [x] Add a reproducible committed line counter with project, generated, and inherited-source attribution.

## Design parity

- [x] Compile the checked-in design reference into the renderer instead of hand-writing an approximation of it.
- [x] Guard the compiled output against drift with a byte-identical recompile check.
- [x] Reproduce every audited declarative binding in the compiled renderer.
- [x] Bundle the design's type and icon families locally so the renderer fetches nothing at runtime.
- [x] Derive the navigation catalogue, the documentation set, and the static site catalogue from the design rather than from three divergent hand-written lists.
- [x] Add reference-versus-built captures and visual diffs, so destinations can move from `compiled` to `verified`. **All 32 destinations now have a capture on each side, a visual diff, a region ledger and a chrome-parity record, About included**, and every one of them came from a single run per side against one build of this tree — so each destination's rectangles and its pixels are the same moment of the same render, which removes the two provenance gaps the previous pass had to argue around rather than close. **The two pieces this line named as remaining are both done.** About's 32nd record exists: the built application settled on the design's own `About` heading and was photographed, measured and compared like the other thirty-one, so the expectation recorded in item 22 is discharged. And the shell-geometry question — repaired in the application, in the design, or in the capture harness — has an answer, which is **the capture harness, twice**, because the single cause this line asserted was not one cause and two of its three parts were defects in the equipment rather than in either artifact. **One: the reference document was never given the height its own root style needs.** The design's root is `height:100%; overflow:hidden`, exactly like the built shell, and a percentage height against an auto-height body computes to `auto` — so the reference shell grew to its content, 622px to 7668px tall, and scrolled, taking 12px off the width on 20 of the 32. `design/support.js` ships that stylesheet in its own `FULL_PAGE_CSS` constant but withholds it when the export declares a `$preview`, which this one does, leaving the sizing to a frame the design tool would have supplied and this harness did not. It is now served with the hosted design, read out of `support.js`'s own declaration rather than typed. **Two: every built capture was taken behind the update banner.** The updater raises it when its background check finishes, not at startup, and the driver dismissed once before the first destination — so a whole 32-destination run was taken with the application's shell 43px down the frame, then 52px as the banner's text rewrapped for a newer version. Nothing failed and every capture looked normal. The banner is now dismissed and *proved* dismissed before every destination, and any built measurement whose shell is not at the window origin is refused outright, naming what sits above it. **Three: the brand cell is 7px wider, and that is not geometry at all.** `Material Asterisk` measures 106.63px against the design's `Asterisk Console` at 100.27px, same font, same padding, same glyph, same gap — a deliberate rename recorded in `compile-design.mjs`'s `BRAND` table, of the same kind as the sample data this project removed. Repaired nowhere, on purpose, and left inside the compared region so it is read rather than masked. **What the repairs are worth, measured:** every shell on both sides is now exactly 1440x1000 at the window origin; five of the eight declared areas measure the *same rectangle* on both sides on all 32; the whole-frame diff fell from 47.13%–63.95% to 23.07%–60.98%; the compared-region diff from 6.67%–26.78% to 6.34%–14.95%; and the compared fraction is now identical at 29.57% across the set instead of drifting. **What ticking this does not claim.** No destination moved to `verified` and none could. All 32 still report a real chrome divergence, and the Material Design 3 audit still reports all 32 nonconforming, so both prerequisites the guard names remain unmet — for measured reasons rather than absent ones. The captures and diffs this line asked for exist; what they now measure is the product's real distance from the design rather than two faults in the equipment measuring it. Evidence: `console/docs/evidence/design-parity-chrome-bar.md`, and `captureContract.geometryDivergence` in `console/inventories/design-parity.json`.
- [x] Define a parity bar that can actually be met: compare chrome and layout with the data-bearing regions excluded, since the reference shows invented sample content exactly where the built application shows the target's real readings. **The bar exists, is guarded, and has been applied.** It is declared once for the whole application as `chromeParityBar` in `inventories/design-parity.json` — tolerance exactly `0`, minimum compared fraction exactly `0.25`, eight measured areas of which two (`statusCell`, `contentPane`) are declared data and six chrome, each role carrying the reason it rests on. `scripts/design-parity-chrome.mjs` applies it, `docs/evidence/design-parity-chrome-bar.md` explains it, and 21 tests plus 19 planted breaks in `scripts/negative-design-parity.mjs` hold it — 11 of those breaks are the bar itself, including softening the tolerance away from exact, lowering the floor, dropping an area, and reclassifying the navigation rail as data so that a divergence there would quietly stop counting. This pass additionally broke the comparator's own floor by hand, forcing `if (comparedFraction < minimumComparedFraction)` to `if (false)`: two tests went red, and green again on restore. A mask wide enough to hide the artifact really is refused rather than passed, which is the one property the whole bar rests on. **What ticking this does not claim.** No destination currently *meets* the bar. All 31 records read `diff`, with 6.67%–26.78% of the compared region differing. That is not a defect in the bar, it is the bar doing its job: where whole-frame comparison could only say "57% of pixels differ", this names one cause. The built shell is exactly 1440x1000 on all 31 destinations, while the reference shell is 1428 wide on 20 of the 32 and ranges from 622px to 7668px tall, so every horizontal position in the top strip drifts — which is why `brandCell` differs by exactly 15.6% and `menuCell` by exactly 12.0% on *every* screen, identical figures being the signature of one divergence rather than 31. Repairing that, and taking About's still-absent 32nd record, belong to the item above.
- [x] Perform the per-destination Material Design 3 conformance audit the `verified` guard also requires. **Performed and committed for all 32 audited destinations.** The objection that had held it back for several passes — that a machine-written `conforms: true` nobody performed would be an invented verdict — was right, and it shaped the auditor rather than preventing it. `scripts/design-parity-material.mjs` measures seven properties of the real rendered destination against published specification values: the 15-size type scale, the four icon sizes, the six-step shape scale, two-layer elevation, translucent state layers with a focus state, the 48dp touch target, and the motion tokens. `conforms` is computed as `defects.length === 0` from findings taken out of the markup; no argument, option or code path can set it, and every check can only ever *add* a defect, so it can understate a divergence but can never manufacture a conformance. `scripts/audit-design-parity-material.mjs` renders each destination from the real `App` class — the product renderer, not the bare compiled shell — with both stylesheets the built renderer loads, and `--check` re-derives every record and fails when the renderer has moved, wired into `test:inventories`. 31 tests and 17 planted breaks hold it. **What it found: 0 of 32 conforming, 8279 divergences across seven checks** — 2364 icon glyphs off 20/24/40/48dp, 1955 type sizes off the scale, 1274 interactive elements under 48dp, 1120 opaque colour swaps where a state layer belongs, 955 off-token durations and easings, 578 radii off the shape scale, 33 single-layer shadows where an elevation is two layers. **No row moved to `verified` and none could**, which is the honest result rather than a disappointing one: this is now a second *measured* reason nothing is verified rather than an absent prerequisite. **What it does not claim, recorded in every record's own `notMeasured` field:** it reads declarations, not pixels, so it cannot decide whether a control whose numbers are right is a real Material Design 3 component or a lookalike sharing them, cannot see geometry that only exists after layout, cannot see runtime-injected style, cannot watch motion run, and measures no colour tokens at all. `docs/evidence/design-parity-material-audit.md`.
- [x] Decide whether `commandCell` becomes a data area of the chrome-parity bar. **Decided: yes, and it is.** The cell renders `connLabel` and `connUptime` — `this.target.label` and `this.target.detail`, which are what the console's own discovery reports about the target it found (`no target` / `nothing discovered yet` with none configured; the discovered distribution name and `N local target(s), connection verified` once one answers) — while the design invents `pbx-hq · AMI 5038` and `up 14d 06:22` in the same two spans. That is the bar's founding sentence word for word, so the cell is data in the sense `contentPane` is. It is **not** the `brandCell` or `sectionList` case, which is the objection worth answering: those two are also guaranteed to differ and stay compared because they differ where this product renders different *chrome* from the design's chrome, and reporting that is what the bar is for. This cell differs because the design invented a *reading*, which is what the bar is for excluding. One thing changed, as the item asked: the pinned role in `inventory-validation.mjs`, then `--side=chrome` re-run alone against the exact build output the captures were taken from — the newest build mtime it recorded is `1787691669082.816` against the superseded records' `1787691669082.8162`, so its staleness check compares the same two things and means neither more nor less. No capture was retaken and no rectangle re-measured. **Measured cost:** compared fraction exactly 29.5717% → exactly 28.0883% (404,472 of 1,440,000 pixels), above the 25% floor; compared-region divergence 6.34%–14.95% → 4.62%–13.68%, which is *not* an improvement in the application, since nothing about the built artifact changed between those figures. **Two predictions in the old wording turned out wrong and are recorded because they did.** Removing the worst area did not leave one uniform worst area behind: it is now `brandCell` on 21 destinations, `tabStrip` on 7 and `sectionList` on 4, where before it was this cell on all 32 — the single identical figure was hiding a spread. And `menuCell` **rose**, 12.00% → 12.28%, on the same 1,886 differing pixels: the union rectangles overlap, so excluding this cell clipped nine columns of *matching* pixels off `menuCell`'s compared strip. Excluding an area can raise a neighbour's percentage by removing agreement rather than by finding disagreement. **What ticking this does not claim:** no destination moved to `verified` and none could — all 32 still report a real chrome divergence and the Material Design 3 audit still reports all 32 nonconforming. `console/docs/evidence/design-parity-chrome-bar.md`.
- [x] Decide whether `statusCell` stops being a data area of the chrome-parity bar. **Decided: yes, and it is chrome now.** The description already pointed that way — the fourth top cell is the Beginner/Expert mode picker, the confirmation-credits pill, the command-palette button and the three window controls — but the description is not what settled it. The renderer did: `App.tsx` overrides exactly **two** values in the whole top strip, `connLabel` and `connUptime`, and **both land in `commandCell`**. Nothing inside this cell is written by the product at all, so there is no invented reading here for the bar to exclude and its exclusion was a narrowing nobody had argued for. One thing changed, as the item asked — the pinned role in `inventory-validation.mjs` and the declaration beside it — then `--side=chrome` was re-run alone against the exact build output the captures came from, no capture retaken and no rectangle re-measured. **Measured cost:** compared fraction exactly 28.0883% → exactly **29.1106%** (419,192 of 1,440,000), the first move of an area that *widened* the comparison rather than narrowing it; compared-region divergence 4.62%–13.68% → 4.80%–13.54%, the low end rising and the high end falling; and, unlike the `commandCell` move, **no neighbouring area shifted by a single pixel**, because this cell's union spans columns 1072–1440 and overlaps nothing. **What it found is the reason it was worth doing.** The cell was expected to match — one compiled template, nothing overridden, the identical rectangle on both sides — and it does not. It differs by **1,420 of its 14,720 pixels, 9.6467%, the same count on every one of the 32**, which is one cause rather than 32. Locating those 1,420 gives two, and neither is noise. **One: the Material Symbols glyphs.** Every differing pixel outside the mode picker sits on an icon, and the discriminator is inside the same pill — the Roboto digit beside the `confirmation_number` glyph is **byte-for-byte identical, 0 differing pixels**, while the glyph differs by 129. Both sides are served the same local `woff2`; what differs is that `font-variation-settings` appears **zero** times anywhere under `design/` and **exactly once** in the built renderer, in the `.msym` rule the compiler adds, pinning `FILL 0, wght 400, GRAD 0, opsz 24` on a variable font the design requests as `opsz 20..48, wght 100..700, FILL 0..1, GRAD -50..200`. **Two: the mode picker's border.** 548 of the 946 pixels differing inside the picker are its own 1px border: crisp on a single row on the reference, split across two rows on the built side, so the built side draws the 28px box half a pixel higher. Not a whole-frame offset — shifting the built region by ±1 or ±2 pixels raises the divergence in all six runs, so `dx=0` is already the best alignment. **What ticking this does not claim.** The icon cause is measured to its mechanism but **not demonstrated**: nothing was re-rendered with the axes changed. The border cause is measured only to its symptom and its origin is **unknown** — the strip is 40px and the picker 28px on both sides, and `(40−28)/2` is an integer. Neither is repaired here; both are items below. No destination moved to `verified` and none could — all 32 still report a real chrome divergence and the Material Design 3 audit still reports all 32 nonconforming. And one mistake this pass made, recorded because the evidence is weaker for it: it reached the capture-provenance build output through a directory junction, and a routine `npx tsc -b` wrote through that junction and restamped all 93 files of `console/dist-electron` in the linked worktree holding it. Nothing tracked was touched and a rebuild there restores it, but the final chrome run therefore walked `console/dist` alone — 21 files, the renderer output the captures actually photographed — so its recorded `newestBuildSourceMtimeMs` is the same `1787691669082.8162` as before while considering fewer files than the run it replaced. `console/docs/evidence/design-parity-chrome-bar.md`.
- [x] Decide whether the compiler should stop pinning the Material Symbols variation axes. **Decided: yes, and it does not any more.** The item asked for the cause to be demonstrated rather than inherited as named, and demonstrating it dissolved the objection that had held it back. **The premise behind that objection is false.** The pin did not arrive with the 49-face font download: it arrived in this compiler's *first* commit, `9beed2f159`, **thirty minutes before** the download in `0611732d0`, and the line was never touched again. It is Google's own documented Material Symbols snippet, carried in unchanged. **What it did, measured against the shipped font rather than argued from the stylesheet URL that requested it.** A new `scripts/woff2-fvar.mjs` reads the face's own `fvar` table: four axes, defaulting to `FILL 0`, `GRAD 0`, `opsz 24`, `wght 400`. Three of the four pinned values were the file's own defaults and did nothing at all. The fourth did a great deal — CSS `font-optical-sizing` defaults to `auto`, which drives `opsz` from the used font-size, and `font-variation-settings` outranks it, so a fixed `opsz 24` replaced every icon's own optical size with a 24px icon's. The design draws 175 icons and four of them are 24px. **The demonstration the item asked for.** `scripts/design-parity-msym-axes.mjs` renders all 98 distinct literal (size, ligature) pairs the design draws, four ways, in one Chromium at the capture tuple's own metrics, from the shipped font file. 95 of the 98 differ under the pin, changing 11,252 pixels; the three that do not are exactly the 24px ones; pinning `opsz` per icon at `clamp(size, 20, 48)` instead reproduces the design's own rendering to **zero** differing pixels across the whole frame, which identifies the mechanism rather than merely correlating with it; and with the pin gone the shipped `.msym` rules draw the design's icons to **zero** differing pixels. **The full two-sided capture run the item required is behind it** — 32 of 32 on each side, one pass each, against one build of this tree, the reference side reaching no network (710 requests to the capture host, 64 font stylesheets answered locally, 0 blocked). **And it turned up something the item did not anticipate, which is the part worth reading.** Retaking the reference side changed how it renders, because it used to run under headless Edge while only the built side ran under Electron — two artifacts compared at a tolerance of exactly zero, drawn by two different browsers in two different modes. `scripts/design-parity-msym-destination.mjs` separates the product change from the harness change across all 32 by comparing both reference sets against both built sets. On the navigation rail — 81,136 compared pixels of nothing but icons and their labels — the baseline is 2,401–6,676 differing pixels, the pin removed *alone* is **worse** at 3,346–7,432, the reference retaken *alone* is worse still at 4,457–8,574, and both together are **0–4,411, with an exact zero on 12 destinations**. Either change alone makes it worse on every destination but one (the exception is `codecs`, named rather than absorbed into a "most destinations"), and only both together converge. **A pass that had removed the pin and kept the committed reference captures would have measured a correct repair as a regression and, quite reasonably, backed it out.** **What it cost and bought:** `statusCell` 1,420 → **555** on all 32; `brandCell` 1,002 → 846; the rail byte-identical on 12 of 32 where it was never below 2,401; compared-region divergence 4.80%–13.54% → **2.95%–12.20%**; compared fraction unchanged at exactly 29.1106%; worst-area tally unchanged at `brandCell` 21, `tabStrip` 7, `sectionList` 4. `menuCell` moved 1,886 → 1,888, which is the control working: it holds no icon, so the pin could not reach it. **What ticking this does not claim.** No destination moved to `verified` and none could — all 32 still report a real chrome divergence and the Material Design 3 audit still reports all 32 nonconforming. The 555 pixels still differing in `statusCell` are **not explained**; they sit in three column runs matching the check glyph and the two labels. The rail's remaining divergence on the other 20 is unexplained too. `master` gained an IAX2 destination and a further screen repair after the build these captures came from, and this pass re-photographs neither — the same condition `master`'s own Fax commit left behind, which retook no capture either. Four guards were each broken alone and watched go red, then green on restore: the pin put back into the compiled stylesheet (two tests red, plus the existing design-drift guard), the design document given a `font-variation-settings` of its own, the `.msym` matcher loosened so a prefix could satisfy it, and the recorded convergence falsified from 0 to 1. `console/docs/evidence/design-parity-chrome-bar.md`.
- [x] Find where the mode picker's half pixel enters. **Found, and it is a third of a pixel rather than a half: it is a font weight, not a box offset, and it enters through a default nobody chose in the design.** The premise this line carried needed restating before anything could be done with it, and the restatement was right. The border symptom it described is gone, killed by the axis-pin pass retaking both sides under one browser, and this pass confirms that by measurement rather than by repeating it: every pixel that still differs inside `statusCell` lies in one of three column runs, and the picker's own 1px border, the boundary between its two halves and every other box edge in the cell are byte-for-byte identical. The three runs are the `check` glyph (1088-1098, 31 pixels), the `Beginner` label (1106-1154, 329) and the `Expert` label (1180-1214, 195). Everything else in the cell matches exactly, including the credits pill's own Roboto digit and all five Material Symbols glyphs. **The cause.** `App.tsx`'s appearance system writes its own default `font-weight: 500` onto the shell root at startup, from `currentAppearanceValues`' `str('ap_weight', '500')` through `applyAppearanceToDom`, so every element in the built application that inherits its weight renders at 500. The design declares the *same* 500 default for the *same* control and feeds it only to the Appearance screen's preview swatch. `Expert` is the only text in this cell that inherits its weight, since the active button, the credits pill and every icon set their own, which is exactly why it is the only one whose ink changes and why every other glyph here is identical. **And the offset falls out of that rather than being a second fault.** At 500 the `Expert` label measures 0.359375px wider, so the picker measures 0.359375px wider; the status group is packed against the right edge of the strip, so the picker's right edge is pinned at 1228.171875 on both sides while its left edge moves 1072.09375 to 1071.734375, taking the check glyph and both labels with it by exactly the same 0.359375px. The credits pill outside it does not move at all. **That is also the answer to why `(40 - 28) / 2` looked like it had nowhere to come from:** every one of those rectangles still rounds to the same painted device pixel, 1072.09375 and 1071.734375 both painting at 1072 and the internal boundary at 1167 and 1166.640625 both painting at 1167, because Chromium snaps a painted box to whole device pixels and positions text at sub-pixel precision. Reading the geometry could never have found this; only the glyphs carry it. **Demonstrated, not asserted, and re-checkable without a browser.** `scripts/design-parity-statuscell-text.mjs --reproduce` renders the checked-in design as the top-level document and photographs it twice, once as it stands and once with the four declarations `applyAppearanceToDom` writes applied to its shell root; both frames are committed. `--check` reads them back and re-derives the whole claim from committed bytes: 555 on all 32, the as-designed frame equal to the committed reference capture in `statusCell` to **0** pixels, and the appearance-defaults frame equal to the committed **built** capture to **0**. **Two hypotheses were falsified on the way, recorded so nobody pays for them twice.** The capture harness photographs the reference through an `<iframe>` inside a `transform: scale(1)` wrapper and the built side through no such thing, which is precisely the shape of thing that moves text by fractions: rendering the same design both ways in one session gives zero differing pixels here, so the wrapper contributes nothing. And the design's runtime wraps every interpolated value in a `<span class="sc-interp">` where the compiled renderer emits a bare text node into the same flex container: replacing the spans with text nodes changes no rectangle and no pixel. **What ticking this does not claim.** Nothing is repaired. The default is still 500, and the built application still renders every inheriting weight one step heavier than the design draws it; changing that would move type on every screen and invalidate all 64 committed captures, which is a product decision rather than a measurement and is not made here. It measures `statusCell` only: the same root declaration reaches every inheriting weight in the frame and `applyAppearanceToDom` writes three more declarations beside it, a colour, a family and a size, and how much of the other seven areas those account for is not measured. No destination moved to `verified` and none could, since all 32 still report a real chrome divergence and the Material Design 3 audit still reports all 32 nonconforming. No destination capture was retaken. Eight guards were each broken alone and watched go red, then green on restore, and one of those breaks found a real gap: deleting the built-capture comparison on its own left both the suite and the negative regression green, because a sibling comparison caught the same planted lie, so that one line now has an assertion of its own that names it. `console/docs/evidence/statuscell-text-pixels.md`.
- [x] Give the built About destination the design's own heading. Its `<h1>` now reads exactly `About`, so the settle condition that proves a driver arrived can be satisfied. The console's name did not disappear; it moved into the About screen's body, where a renamed console still shows its chosen name and, once renamed, states outright that a bug report will still name the shipped product. That is a better home for the boundary than the title bar was. Two tests read the `<h1>` itself rather than the flattened screen text, because `strip()` runs the heading and the subheading together into one line and would not notice a name moving between them. **What this item did not do is take the capture.** The approved off-screen capture route was unavailable to this pass, so `about-built.png` and `about-diff.json` are genuinely still absent rather than quietly regenerated, and the built count stays 31. The next `--side=built` run should now settle on About like the other thirty-one; that is an expectation, not a result, and `inventories/design-parity.json` records it as one.

## Real readings, no sample data

- [x] Read live channels, endpoints, contacts, registrations, queues, modules and uptime from allowlisted read-only Asterisk CLI commands.
- [x] Draw the dialplan canvas from the target's own `dialplan show` output.
- [x] Remove the design's sample rows, dashboard tiles, health bars, nav badges, history, agent-rail and trunk-authentication content from the running app.
- [x] State the exact reason a surface is empty instead of showing an invented value.
- [x] Read per-endpoint transport and codecs, so those columns stop reading `—`. Shipped in `0d7606337` and `c70cd71e3`; this line was left unticked when the pass that wrote them ended, so what follows is the verification that was owed rather than a fresh implementation. **What it does.** The plural `pjsip show endpoints` prints neither value, because `ast_sip_cli_traverse_objects` sets `show_details_only_level_0` from `!is_container` (`res/res_pjsip/pjsip_cli.c` line 151), so only the singular `pjsip show endpoint <id>` reaches `ast_sip_cli_print_sorcery_objectset` and its ` %-*s : %s` parameter rows. The console now runs that command per endpoint, at most 100 of them, six at a time, and parses `transport` and `allow` out of the table. Three distinctions the old dash could not draw are now drawn: an endpoint pinned to a transport the target does not have reports the name it is pinned to instead of looking like one that pins nothing; `(nothing)` reads `none allowed`, because an endpoint that allows no codec and an endpoint nobody asked about are different facts; and where only the live-channel reading is available the cell says `ulaw (in use)`, since one codec on one call is not the list an endpoint offers. Anything past the budget is named in `notRead`, and the screen carries a note saying which endpoints those are, or why the read failed. **What this pass verified rather than inherited.** The full suite is green at `EXIT=0` (3436 tests across all ten groups) and `npx tsc -b` is clean. Both ends of the wiring were then broken deliberately, one at a time, since a reading taken and dropped on the way to the screen is this repository's oldest recurring defect and produces no error and no failing test: the dispatcher's `readEndpointsView` routing commented out gave 1 red, and the detail argument dropped from `rowsFor` gave 3 red. Each was restored and watched go green, and each break left the real line in place as a comment, because a substring needle is satisfied by a commented-out call. Both guards are anchored to a whole line and refused it. **What ticking this does not claim.** Nothing here has been run against a live Asterisk. The parser is proved against the format strings in this checkout's own sources and against fixtures built from them, and no more than that; a real exchange is the item two lines below and remains open.
- [x] Wire real sources for the history, agent and trunk-authentication surfaces. **Every one of the nine screens across those three families now reads a real source or names the exact store it has not got, and none of them borrows the PBX's reason any more.** **History was already wired** and is now guarded rather than merely believed: it reads the console's own append-only local git store through `local-history.list`, and two whole-line anchors hold that — one on the `refresh()` call and one on the render — because a substring needle for `local-history.list` is satisfied by a commented-out call, which is how a wiring line usually dies. A third holds the distinction the screen's two sentences carry, since "nothing has been read yet" and "it was read and is genuinely empty" are different facts and conflating them makes a real reading indistinguishable from never having taken one. **The agent rail's seven destinations each name their own source now**, from a hand-written record pinned against the rail's real membership in the compiled design, so an eighth destination arriving without an entry fails rather than inheriting the old wording. That wording is the defect this half repairs: six of the seven used to answer "No target is connected" on a first run — true, irrelevant, and read as a promise that discovering a phone system would fill them in. It would not; nothing on this rail reads a target at all. **Two of the seven have a real source and now read it.** Operations & releases renders this build's own bundled release history — real `ding-pbx-console-v*` tags, real dates, at most twenty, from `scripts/bundle-changelog.mjs` — and leaves Artifacts, Duration and State empty *and says why*: a tag is not a release, and nothing in this process has asked a forge whether one was published, so writing `Published` into every row would assert exactly that unchecked fact. Vocabulary reports the real loaded state of the uploaded dictionary and **deliberately does not tabulate its terms**, because every table here can be selected, copied and exported to a file and a private vocabulary term must never reach an export or the clipboard; that is a recorded decision, not an omission. The remaining five have no store at all — no memory corpus, nothing syncing one, no skills registry, no opened hub session, no stored secret — and each says which. **Trunk authentication gained a genuinely new reading**, and it is the first thing that screen has ever had to say about a target: `pjsip show auths` names every `type=auth` object the target really has with the `username=` it presents, `pjsip show registrations` is read alongside it, an object with no username reads `no username set` rather than an empty pair of brackets, and a failed read carries the target's own reason. The parser is written against this checkout's own `res/res_pjsip/config_auth.c` format strings, and its fixture re-derives the CLI column widths from `include/asterisk/res_pjsip_cli.h` rather than trusting the numbers written beside it. **The safety property that reading rests on is asserted, not assumed.** The plural command is a container walk, so `pjsip_cli.c` leaves `show_details_only_level_0` at 0 and the objectset is never printed; the singular `pjsip show auth <id>` *does* print it, and `password`, `md5_cred`, `oauth_secret` and `refresh_token` are all registered fields on that object, so running it would put a real credential on screen. It is in neither allowlist and the gateway refuses it if it is constructed by hand, and both are tested. **What ticking this does not claim.** The partner-request inbox is still empty and still structurally so: there is no protocol, no transport and no partner identity behind those cards, and choosing each is a product decision rather than a piece of wiring — so it says that, in both the connected and disconnected states, because connecting a phone system would not add one request. The auth objects are deliberately **not** rendered into the design's "Answer history" grid: its four fields are partner, what, answer and when, so a row there claims a partner asked and this console answered, and an auth object is neither — putting real-looking content under a label it does not belong to is the same defect as the sample rows this project removed, and a guard refuses it. Nothing here has run against a live Asterisk; the parser is proved against this checkout's own sources and fixtures built from them, and no more than that. **One measured cost, and one new coupling, both recorded because they are real.** Filling the Operations table added **179 Material Design 3 divergences** to the audit (8,458 → 8,637: typeScale +99, iconSize +20, shapeScale +20, touchTarget +20, motion +20) — not a regression in this change but the honest measurement of a screen that now has twenty rows of the design's own nonconforming row markup where it had none. And because those rows come from a build-generated file, `npm run build` regenerating `changelog-bundle.ts` from a new tag now makes that one audit record genuinely stale; `docs/agent/ops.md` names the single command that refreshes it. No other destination has that coupling. Nineteen breaks were each planted alone, watched go red and restored green by `scripts/negative-real-sources.mjs`, wired into `test:inventories` — and it earned its place twice on the way. Three breaks did not land on the first pass because a `\n` needle cannot match a CRLF file, and the script's own "did the bytes actually change" assertion caught that rather than three guards appearing to pass. Then one break that *did* land stayed green, and it was the test that was wrong: it sized its fixture from the very constant it was checking, so raising the listing bound to a thousand still left exactly three objects over and the assertion agreed with any value at all. The bound is asserted before it is used now.
- [x] Verify every reading against a live Asterisk target. **All 27 readings now parse a running Asterisk's real output, and 23 of them do it with real rows rather than against an empty exchange.** Until this pass no reading in this console had ever been taken from a running Asterisk: every parser was written against a format string in this checkout's own C sources and tested against a fixture built from that string, which is a real discipline and a *different claim* -- it says the parser matches what the source says Asterisk prints, not that it matches what Asterisk printed. **What ran.** All 63 allowlisted read-only command lines, plus the one object command, through `LocalAsteriskCliGateway` over `NodeProcessExecutor` -- the production read path, not a re-implementation -- against the `ding-pbx-console` WSL distribution, with every stdout committed verbatim. Then, because the exchange as provisioned has no configured objects at all (every shipped sample file is comment-only, so twelve readings would otherwise have been verified against `No objects found.`, which proves the command runs and proves nothing about the parser), a fixture was written across seven resources through the console's **own** `StructuredConfigPlanner` -> `ConfigTransaction` -> `WslConfigTransport` path, Asterisk was reloaded, and every command was run again. Beside the parsers, all **14 gateway-backed production readers** were driven exactly as a screen calls them and every one reported `available` -- the half a parser test cannot reach, since a correct parser behind a reader that reports `unavailable` is a blank screen. Then every backup handle was rolled back and each of the seven files hashed on the target again: **all seven came back byte-identical**, which is a measurement rather than a claim about what `cp` is supposed to do. **The four readings that stayed empty are exactly the four the harness declares it cannot populate**, each with its reason recorded rather than left as an unexplained zero -- a channel and a channel statistic exist only while a call is up, `confbridge list` prints conferences that are *running* rather than rooms that are configured, and an ARI application appears when a client connects rather than when a file says so. The check refuses any *other* reading landing in that list, so a reading that quietly stopped returning rows cannot pass as a documented limitation. **11 of the 63 commands are not built into this target and the console handles all eleven correctly**, diverting on `No such command` rather than parsing a refusal into an empty table. **Three things it found, all recorded as items below and none repaired here.** The write path cannot represent a file that repeats a section name: an *unchanged* round trip of the `[6001] type=endpoint` / `[6001] type=aor` pattern nearly every real `pjsip.conf` uses rewrites the endpoint as a second copy of the aor and deletes two of its keys -- it fails safe, because the transaction's post-read catches it and rolls back, which is exactly what happened to the fixture's first attempt. The voicemail reading drops a mailbox whose field overruns its fixed-width column and the screen shows the shortened list with nothing to say a row is missing: the target's own trailer said 4, the reading produced 3. And `media cache show` is allowlisted without the argument it requires, so the target's `Usage:` line reaches a screen as a successful reading. **One thing worth knowing that is not a defect:** the baseline `dialplan show` disagreed with `extensions.conf` as it stood on disk, because Asterisk had not reloaded `pbx_config` since an earlier session restored that file -- so `dialplan show` reads *loaded* state rather than file state, and the console cannot presently tell an operator when the two have diverged. **What ticking this does not claim.** Nothing here writes to an exchange anybody depends on; that is still the annotated item below. The live half is a fact about one moment: `--check` re-derives the parse half from the committed captures on every machine and cannot re-run the gateway, the executor, the reload or the restore, and the ledger's `notVerified` says so. The reload is harness machinery, not a console capability -- the control plane has no reload action. 21 tests and 12 planted breaks hold it, each break planted alone, watched go red, and restored green -- and two of those twelve had to be rewritten, because removing a guard whose condition currently holds can never go red and reads exactly like a guard that is watched. `console/docs/evidence/live-readings.md`.
- [x] **Let the write path represent a file that repeats a section name.** **It can now, and the exact text this line named round-trips byte for byte.** The defect was reproduced before anything was touched, and it reproduced precisely as recorded: `renderConfigOver` built its desired map with `wanted.set(section.name, section.entries)`, so on an *unchanged* round trip of `[6001] type=endpoint context=default allow=ulaw` followed by `[6001] type=aor max_contacts=1`, `type = endpoint` came back as `type = aor`, `context` and `allow` were deleted, `max_contacts` moved into the first section, and parsed entry counts went `[3, 2]` → `[2, 2]`. **The repair is the rule the same function already used one level down.** It matched repeated *keys* by position — several `allow=` lines, many `exten =>` lines — and matched repeated *sections* by name alone; the desired value is now grouped into one ordered list of entry-lists per name and the nth `[name]` in the file is matched against the nth desired section of that name. Editing, adding to, dropping and appending an occurrence each reach the occurrence that asked for it, verified individually rather than inferred from the round trip. **What it deliberately does not solve, recorded in the function's own comment rather than left to be discovered:** occurrence matching is positional, and a section carries no identity beyond its name and where it sits, so deleting the *first* `[6001]` and keeping the second is indistinguishable from editing the first into the second — the survivor is rendered over the first block's text and keeps *that* block's comments. The configuration written is correct; which comments travel with it is decided by position, exactly as it already was for a repeated key. **Six tests in `console/tests/control-plane/config-round-trip.test.ts`, and the repair was broken four ways, one at a time, each watched go red and then green on restore:** the desired map keyed by name again (3 red), the occurrence lookup pinned to `[0]` (5 red), the trailing append loop returned to name-based skipping (1 red), and the leading nameless region left unclaimed (1 red). Each break was grepped for after applying, because an edit that never landed reads exactly like a guard that held — and the fourth break exists because the leading-region code path was otherwise unguarded, which is the vacuous-guard shape this project has been bitten by before. **What ticking this does not claim.** Nothing here ran against a live Asterisk: the repair is proved against fixtures and this checkout's own round-trip contract and no further, so the live half of this claim is still owed by whichever pass next runs `--capture`. The live fixture still avoids the shape and `mergeFixture` still refuses a duplicate name — kept deliberately, because the committed captures were taken under that constraint and widening it without re-running against a target would describe a run that never happened, though its stated reason is now the honest one (which section to fold into is ambiguous) rather than the stale one. The ledger's own finding is left exactly as that run recorded it, with the repair noted beside it rather than written over it. Suite green at `EXIT=0`, 3508 pass, 0 fail; `npx tsc -b` clean. `console/docs/evidence/live-readings.md`.
- [x] **Say when a reading dropped a row.** Shipped in `9973496bf` and merged in `b58c70892`; the pass that wrote it ended before it reached this line, so what follows is the verification that was owed rather than a fresh implementation. **What it does.** `parseVoicemailUsers` now hands back `dropped` beside `users` and `total` -- every data line it refused, verbatim -- and both screens read the count they were throwing away. Voicemail says *1 of the 4 voicemail users on this target is missing from this table*, names the mechanism, and quotes the line: `"myaliases 1234@devices 0"`. AMI says the same from its trailer alone, and deliberately gets no `dropped` list, because `parseManagerUsers` cannot name the line it lost -- a username of nothing but hyphens is indistinguishable from the report's own separator, so a count is the whole honest signal it has. The note trusts the trailer over the parser: `total - parsed` is how many rows are genuinely absent, `dropped.length` is how many lines this parser choked on, and a note driven by `dropped` alone would announce a missing mailbox that was never missing. A reading that never answered now names itself on either screen too -- both edit a configuration file, so `note()` returned from its configuration branch and never reached the reading-failure report at the bottom. **What this pass verified rather than inherited.** Full suite green at `EXIT=0`, 3573 pass, 0 fail, zero `not ok` lines, re-run on `master` at `b58c70892` before this line was ticked. The 18 breaks in `scripts/negative-dropped-rows.mjs` are wired into that run and all 18 reported red-then-green, including the two that earned their keep: one made the AMI screen claim a shortfall for a reading that had *failed* and stayed green because the break was unreachable rather than unwatched, which exposed the genuinely unguarded property beside it -- a screen fed by two commands, one failing while the other comes back a row light, used to report whichever sentence came first and drop the other. **What ticking this does not claim.** Nothing here ran against a live Asterisk. It is proved against the committed captures, the fixtures and the render tests, and no further. The evidence ledger's parse half was re-derived from those same committed captures by `--reparse`, which refuses to write when a capture no longer hashes to what was recorded, so no capture was retaken and no live-half field moved.
- [x] **Decide what `media cache show` is doing in the allowlist.** **Decided, and it was neither of the two options this line offered: the entry was not a command missing an argument, it was the wrong command.** `main/media_cache.c` registers two CLI entries whose names are prefixes of one another. Line 528 is the singular `media cache show`, which refuses any `a->argc != 4` and reads its subject from `a->argv[3]`; line 477 is `media cache show all`, the container listing that takes no argument at all. The allowlist wanted the container and had been carrying the singular, which is why a live target answered it with a usage line and exit code 0 that `AsteriskReadings` does not divert on. **What it does now.** The allowlist carries `media cache show all`. `parseMediaCacheItems` reads it, written against the format strings themselves -- the header at line 490, the separator at 491, and `FORMAT_ROW "%-40s\n\t%-40s\n"` at 463, printed at 467 -- so one item is two lines, a URI and then a tab and its local file. The dispatcher takes it for the `moh` view beside the classes (that destination's own feature record declares `tools: ['config', 'media']`, so it is the console's one media surface), and the Music on Hold screen says what is in the cache. It is a sentence rather than rows on purpose: that table's columns are Class, Mode, Source and Tracks, and putting `http://…/hold.gsm` under a column headed Class would claim `musiconhold.conf` names it, which is untrue and is the same defect as the sample rows this console removed. Empty, unread and failed are three different facts and the screen says which. **The singular is deliberately not a second object command.** It would fit the mechanism and prints per-item metadata the listing does not, but its object id is a URI, and `OBJECT_ID` admits no `:` and no `/` on purpose; widening the one check between a target-supplied string and an `asterisk -rx` argument, for metadata no screen displays, is a bad trade. A test fails if that is ever reversed quietly. **Run against a live target, with its own provenance.** `media cache show all` was run through `LocalAsteriskCliGateway` over `NodeProcessExecutor` against the same disposable WSL exchange, empty and populated, and the cache was put back byte-identically. Populating it needed the target to *fetch*: the cache holds what Asterisk retrieved at run time, no configuration file can fill it, and `media cache create` needs a create wizard `res_http_media_cache` does not implement, so it answers `Unable to create`. The harness therefore serves one file over loopback HTTP and refreshes two URIs, one inside the format's 40-column pad and one well past it -- which is what proves the parser rather than asserting it, since `%-40s` has no precision and the long URI arrives unpadded beside a short one padded out to 40. It is recorded in a new `commandsAllowlistedAfterThisRun` section, checked exactly as a phase capture is and additionally refusing a row for a command the allowlist no longer carries; **nothing in the phases, fixture, restore or production-reader records moved**, because those came from a different run against a different exchange state and merging them would describe a run that never happened. **Two findings worth more than the repair.** The first capture run wrote three byte-identical empty captures and *passed its own restore proof*, because after-restore trivially equals before-populate when the populate did nothing at all -- so the harness now refuses a populate that changed nothing. The cause was that `$name` does not survive the trip to the target: something between `spawn` (with `shell: false`) and the Linux side of `wsl.exe` expands a `$`-sigil identifier and replaces it with nothing, even inside a quoted heredoc, so `my $body = 1; my $fh; local $/;` arrives as `my  = 1; my ; local $/;` and only `$/` survives. Nothing reports it; the payload is base64 now. **Verification.** Full suite green, `npx tsc -b` clean, and 18 breaks in `scripts/negative-media-cache.mjs` each planted alone and each watched go red then green, plus one more planted by hand against the new guard requiring every `negative-*.mjs` to appear in the `npm test` chain. **Not claimed:** the parser has never seen a cache item Asterisk did not fetch from an HTTP URI, and the singular form's metadata output is recorded in the evidence article but read by nothing.
- [x] **Tell an operator when the loaded dialplan and `extensions.conf` have diverged.** **It does, in the sentence under the canvas's own heading, on every read.** Diverged: it names the contexts the file declares that are not loaded, the contexts loaded from that file that the file no longer holds, and says that a dialplan reload on the target is what closes the gap. In agreement: it says so plainly and over how many contexts. Not comparable: it reports the target's own reason rather than staying silent and letting the drawing pass for the file. **Both halves come from one run of `dialplan show`** — running it twice would draw the canvas from one moment and judge the file against another, and nothing in either output would say they were different moments, so the reading carries the contexts beside the graph rather than fetching them again. **Every rule is taken from this checkout's own sources rather than from the shape of a sample file, and three exclusions matter more than the comparison itself.** `pbx/pbx_config.c:103` names the registrar `pbx_config` and `main/pbx.c:3895` prints the creator of every context, so **contexts another module created are never compared** — on the committed live capture 21 of 49 belonged to `pbx_ael`, `res_parking` or `func_periodic_hook`, and comparing without that filter reports 21 defects that are not defects. `pbx/pbx_config.c:1895-1901` registers each extension with the basename of the file that declared it, which is the `[file:line]` column, so **a context from an `#include`d file is reported as an include** rather than as a loss; that column is a file only when a line number came with it, because a bare `[pbx_ael]` is a module name and reading it as a filename would invent a file called `pbx_ael`. And `pbx/pbx_config.c:1745-1746` skips `[general]`/`[globals]` by name while `main/config.c:228` marks `[foo](!)` ignored so `ast_category_browse` never returns it — **so neither a settings section nor a template is looked for in the loaded dialplan**. **The file is parsed from exact bytes rather than through the console's ordinary configuration reader**, because that reader keeps only headers ending in `]` and `key = value` pairs: it loses `#include` entirely, has no notion of a template, and drops `[from-internal] ; inbound`. Each of those would surface as a divergence that is not one. The text never leaves the privileged process; only context names, directive lines and counts reach the screen. **The comparison also reports its own shortfall**, from the context total `dialplan show` prints for itself (`main/pbx.c:4135`): when that disagrees with the number of headers this reading made out, the screen says so *before* it says anything else, because an empty list drawn from a short reading must not read as agreement. **One quieter defect fell out on the way and is fixed here.** The canvas declares `file: "extensions.conf"` in the design, so `note()` returned from the configuration branch before ever reaching this screen — and since `read()` answers the canvas through its own `pbx.read` view, `this.configs.canvas` is never populated, so that summary said `Reading…` forever and the canvas's own failure reason was unreachable code. The canvas branch now comes first, and the ordering is asserted rather than assumed. **What ticking this does not claim.** Nothing here ran against a live Asterisk: the parsers are proved against `pbx/pbx_config.c`, `main/config.c` and `main/pbx.c` and against the committed capture at `console/release/evidence/live-exchange/readings/baseline/dialplan-show.txt`, and no further. An included file is never opened, so a context declared only inside one is reported as an include rather than resolved; and an empty context this file does not declare, sitting beside an `#include`, is named unattributed rather than counted as a divergence, because a file this console did not read could account for it. **Verification.** Full suite green at `EXIT=0`, 3664 pass, 0 fail, re-run on the final tree after an earlier run was invalidated by editing the docs bundle underneath it; `npx tsc -b` clean. 31 breaks in `console/scripts/negative-dialplan-divergence.mjs`, each planted alone, watched go red, and watched go green on restore — the registrar filter, the include attribution, the comment stripper, the transport allowlist, the dispatcher wiring and the branch ordering among them. Released as `ding-pbx-console-v0.0.260-r1`, non-draft, targeting `55f7d6e17`, its 446 MB `Ding-PBX-Console-Setup.exe` answering HTTP 200.
- [x] **Say when a reading dropped a row.** Shipped in `9973496bf` and merged in `b58c70892`, ahead of this pass: `parseVoicemailUsers` and `parseManagerUsers` hand back `dropped` beside `users` and `total`, and both the Voicemail and the AMI screens read the count they were throwing away rather than rendering a shortened list that looks complete. The Voicemail screen says *1 of the 4 voicemail users on this target is missing from this table*, names the mechanism and quotes the line it could not read; the AMI screen says the same about `manager show users` from its trailer alone. A reading that never answered names itself too, on both screens. Held by nine render tests in `console/tests/ui/dropped-rows-wired.test.tsx`, reading the sentences out of the real `App`'s markup. This pass's own first attempt at the same problem (`voicemailDroppedNote`, a simpler total-vs-row-count comparison with no dropped-line quoting and no AMI coverage) turned out to duplicate work already on the branch this lane was stacked onto, and was dropped in favour of the already-shipped, more complete mechanism during that stacking merge rather than kept alongside it.
- [x] **Decide what `media cache show` is doing in the allowlist.** **The entry was not a command missing an argument; it was the wrong command.** `main/media_cache.c` registers two CLI entries whose names are prefixes of one another: the singular `media cache show` (refuses any `a->argc != 4`, reads its subject from `a->argv[3]`) and `media cache show all` (the container listing, no argument at all). The allowlist wanted the container and carried the singular. It now carries `media cache show all`, `parseMediaCacheItems` reads it, the dispatcher takes it for the `moh` view beside the classes, and the Music on Hold screen says what is in the cache -- verified against a live target, empty and populated, with its own captures recorded in `commandsAllowlistedAfterThisRun`; see `docs/evidence/live-readings.md` finding 3. The singular stays out on purpose (its object id would be a URI, and widening `OBJECT_ID` for metadata no screen displays is a bad trade).
- [x] **Tell an operator when the loaded dialplan and `extensions.conf` have diverged.** **The canvas screen now compares them.** `contextsMissingFromLoadedDialplan` (`app/renderer/src/canvas.ts`) names every context `extensions.conf` declares that the loaded dialplan graph has no extensions under -- exactly `[dundi-e164]`, `[iax2-trunk]` and `[trunkint]` in the shape this run measured. It only claims the direction a reading can prove without guessing, and `parseDialplanGraph`'s own return shape is untouched, so this needed no fresh live capture either. Fixed alongside it: the canvas screen's status note was separately stuck reporting "Reading…" forever regardless of what `dialplan show` answered, because `canvas` declares `file: 'extensions.conf'` like a config-editing screen but has no bound controls for that branch to ever report on -- so the dedicated canvas branch, and this new note, were unreachable until that routing was fixed too. Held by `console/tests/ui/canvas.test.tsx` and `console/tests/ui/canvas-divergence-wired.test.tsx`. `dialplan show` reads what `pbx_config` has loaded, not what is on disk, and the console presents it as the target's dialplan with no way to tell the two apart. Observed on a live target whose `extensions.conf` contained `[dundi-e164]`, `[iax2-trunk]` and `[trunkint]` while the running Asterisk had none of them, because nothing had reloaded since an earlier session restored the file. A canvas that silently shows a dialplan no file describes is the same class of defect as a reading with no stated source.

## Evidence and completeness

- [x] Add the pinned official FreePBX 17 public module catalog, exact unavailable reasons, hand-written module inventory, and schema-driven native form/action metadata. The catalog generation and static inventory negative regression are green; runtime, built-artifact interaction, and capture evidence remain unverified.
- [x] Add the native FreePBX catalog destination, explicit exclusion records, multi-resource form identity, and typed fwconsole action boundary with readback and safe inverse rollback handling. No live runtime or built-artifact verdict is claimed.

- [x] Record the independent design audit's source hash and exact aggregate counts.
- [x] Add fail-closed schema and completeness validators.
- [x] Add deliberate red-then-green negative regression scripts.
- [x] Map all 32 audited destinations to final reference routes, product routes, and built-artifact captures after integration. **All 32 rows carry the mapping now, and the product route is one the product actually resolves rather than a string that named nothing.** **What was already true.** `scripts/generate-design-parity-capture-manifest.mjs` had mapped every destination to a reference route, a built route, the evidence artifact paths and a real click-sequence navigation plan, and all 32 built captures are on disk and guarded by `design-parity-captures-on-disk.mjs`. **What was not.** The product route was `ding-pbx://destination/{id}` and nothing in `app/` resolved it: no scheme registered, no argument read, no navigation, and the inventory's own `builtRouteStatus` admitted it in a sentence nobody has to read in order to use the file. Two of the three things each row was mapped to were real. A route nobody can follow is not a mapping, it is a plan for one, and it read as the former in every file that carried it. **What it does now.** `shared/destination-route.ts` is the one place the route is spelled, parsed and refused. `app/electron/deep-link.ts` holds a route that arrives before the renderer can take it, and REPLACES rather than queues, because two links clicked before the window opens are two answers to one question and the person meant the second; a later argument that is not a route does not discard the one being held. `main.ts` registers the scheme, takes the single-instance lock (without it a protocol activation launches a second console, which cannot see the first one's window and so cannot navigate it), reads the launch command line and every second-instance activation, and flushes on `did-finish-load`. Both preloads expose the bridge on one channel, `preload.cjs` included, since that is the file `webPreferences.preload` actually names and the typed one has drifted from it before. `App.tsx` resolves the id against the compiled catalogue and calls the shell's own `openScreen`, so the rail moves with the screen rather than leaving another group's section list on display. Parsing and resolving are two functions on purpose: the privileged process must decide "is this even a route" with no catalogue at all, which is exactly what stops an arbitrary `http://` or `file://` argument on the command line ever being treated as navigation. **What it deliberately does not do.** The application acts on the destination alone. `state`, `theme`, `width`, `height` and `scale` are validated so a malformed route is refused whole rather than half-applied, and are then not applied: a link that resized somebody's window or flipped their theme would be a link that edited their settings, and the person who followed it asked to go to a screen. `DESTINATION_ROUTE_APPLIES` records that decision and a test pins its exact contents, so widening it has to be argued for. An unknown id navigates nowhere and names the id it does not have; nothing falls back to the dashboard, because a link that quietly lands somewhere else looks exactly like one that worked. **The mapping itself.** Each of the 32 rows now records `referenceRoute`, `builtRoute` and `builtCapture` instead of leaving all three to be re-derived by whoever needs them, and the validator refuses a row whose values are not the template expansion at the capture tuple. That tuple is now pinned by exact value too, for the same reason the chrome-parity bar is: every recorded route derives from it, so editing it would silently re-derive thirty-two routes to agree with whatever it was changed to while the equality check kept passing. The check that makes the product route a fact rather than a copy is the last one: each recorded `builtRoute` is handed to the parser the running application uses and must resolve to that row's own destination. **Verification.** Full suite green at `EXIT=0`, 3694 pass, 0 fail, against a `master` baseline of 3664 taken before anything was touched; `npx tsc -b` clean. 27 breaks planted one at a time and each watched go red and then green on restore: 15 lies in the inventory, and 12 wiring lines COMMENTED OUT rather than deleted, because a substring needle is satisfied by a commented-out call and that is how a wiring line usually dies. The break helper refuses to report a case it did not actually land, since an edit that never applied reads exactly like a guard that held. Two of the fifteen earned their place: the template-equality check would otherwise have hidden the resolve checks completely, because a row regenerated from a bad template agrees with that template, so only a template producing a WELL-FORMED route to the wrong place reaches those branches, and without those two cases they would have been unreachable code wearing the appearance of a guard. **What ticking this does not claim.** No built capture was retaken through the route; they remain the ones the recorded parity run produced, driven to each destination by rail-then-section clicks. No link has been followed on a real desktop by this pass: the parser, the holding router, the renderer's navigation and the presence of every wiring line are proved, while the main process's own registration is proved only to be present in the source, because driving it needs a real Electron `app`, a real registered scheme and a real operating system to hand it a link. All 32 rows stay `compiled`; nothing here moves a row to `verified`. `console/docs/platform/destination-deep-links.md`.
- [ ] Replace every unverified per-surface inventory field with merged implementation, documentation, localization, local-check, interaction, and capture evidence. Still the largest open item in the project, and still open — but **the measured position this line carried was wrong in every particular and is corrected here**, because it is the sentence the next pass plans against. It claimed 0 of 88 rows verified, all 528 artifacts absent, and four of seven evidence directories not created. The real position before this pass was **2 of 88 verified** and **388 of the 480 artifacts named by the then-unverified rows already on disk**; every evidence directory exists. What was actually missing was narrow and specific, and finding that out was most of the work: implementation, documentation, localization and local-check evidence is present for **every one of the 84 non-exempt rows**, and the entire gap is the two artifacts that require a running program — a built-artifact interaction record and a capture. **This pass closed two of those, by driving the real packaged application rather than by asserting anything.** `scripts/ui-drive/lock-evidence.mjs` launches the packaged executable on an off-screen desktop under a throwaway profile, proves exactly one debugging target before evaluating anything, and drives the per-element lock end to end. For `built-in-authenticator` it establishes three things a unit test structurally cannot, because every renderer test injects its own host: pairing added **no resource entry of any kind**, so the secret really is generated locally and no network call is made; a code computed **independently by the script**, with Node's own crypto, from the revealed secret is accepted by the running application; and a code from three steps back is refused first, so acceptance is time-based rather than any six digits. It also watches `lockNext` refuse to finish an unpaired one-time-code lock. For `unlock-ladder` it locks a screen with a PIN, enters three wrong ones at the real keypad, and observes the first two drawing an ordinary refusal, the third offering a real dish challenge with its four choices, and — the rule the whole feature rests on — the element **still locked after the challenge was graded**. Both rows are now `verified`, taking the inventory to **4 of 88**, with 414 of the 504 artifacts the 84 non-exempt rows name on disk. **Two defects in this pass's own evidence, both found by reading the output rather than by any check, and both now guarded.** The first run recorded the pairing URI's query string whole, so a live authenticator secret landed in a file destined for a commit; the second leaked fragments of the same secret into both PNGs, because the panel revealing it was still open behind the dialog being photographed. Records now carry the secret's length and every parameter beside it rather than its value, the panel is closed and its absence proved before either capture, and `scripts/built-interaction-evidence.mjs` refuses any record carrying a base32 run long enough to be a secret. A third: the first capture run photographed the **onboarding wizard** while every step underneath genuinely worked, which is the trap `scripts/ui-drive/drive.mjs` already documents and which looks entirely healthy from the outside because something did paint — so every capture now clears the wizard and the update banner (which arrives late, after its background check, and so cannot be dismissed once at startup) and then hit-tests the dialog with `elementFromPoint` before the shutter. **What remains, named exactly rather than left as a total.** Three windows-console rows have no built evidence and **three different reasons**: `browser-extension-download-surfaces` is `absent` in the registry because this repository contains no browser extension at all, so there is no surface to drive and the honest resolution is an exemption with a recorded decider rather than a driven record; `forge-publishing` is explicitly blocked on a forge transport, and its own registry note says wiring a control to it would produce a publish button that plans and never publishes, so there is deliberately nothing on screen; `in-context-recovery` is genuinely implemented and consumed on the deploy-failure path, and driving it needs a real provisioning failure, which this pass did not attempt. The other **42 rows are the whole pages-site surface**, and that is not an evidence job at all: 23 of its 44 features are `absent` in `site/feature-registry.json` and 11 more are `partial`, so most of those rows need the feature built before anything can be photographed. `scripts/built-interaction-evidence.mjs` now ties all 39 windows-console records to their pictures by digest, and `scripts/negative-built-interaction-evidence.mjs` refuses 12 planted lies one at a time. **This pass verified no new row, deliberately, and closed the hole that would have let a later one verify thirty-five.** The position is unchanged at **4 of 88**, and the reason needs saying exactly, because the sentence above invites the wrong conclusion: thirty-five windows-console rows already have all six of their artifacts sitting on disk, so the only thing between them and a `verified` status was a status field nobody had edited. Measured rather than assumed. With `collapsible-filters` promoted in the committed inventory, `verify-inventories.mjs --allow-unverified` printed `PASS: 5/88 rows claim verified; 30 claimed evidence artifacts resolved on disk`, `built-interaction-evidence.mjs` printed PASS, and the exemption check printed PASS. Not one existing check objected, and that row's own record says it "captured its Filter row without clicking". **The records are honest; they are simply not proof.** All 39 carry a `notInterrogatedHere` list naming what they never exercised (school-mode's says "actually turning School mode on" was not attempted). Two say in their own words that they clicked nothing. Of the 26 that record `observedPanelControls`, **25 came back an empty list**, 26 record `anchoredToOriginatingField: false`, and only `regex-builder` reports either with content. Fourteen record an element count identical either side of the click they describe. **`console/scripts/operated-interaction-evidence.mjs` now refuses a `verified` row whose record operated nothing.** Such a row must appear in a hand-written per-row map naming the exact observations that prove its feature behaved, and the shape each one has to take: the awkward readings a photograph cannot produce, such as the refusal a stale one-time code drew, or the resource count that stayed at zero across pairing. Where the proof is a click it must also carry a before/after pair that differs, and any record whose own prose says it captured a control without clicking it is refused outright. A row missing from the map cannot be verified, and a proof left behind for a row that has since been demoted is refused from the other side, so the map cannot drift out of step with the inventory it speaks for. `scripts/negative-operated-interaction-evidence.mjs` plants **17 lies one at a time**, each of which turns it red, and the untouched evidence turns it green; unwiring it from the suite was broken and restored too, and `tests/scripts/test-suites-are-wired.test.mjs` named it. **So the next pass's job on those 35 rows is to drive them, not to promote them**, and the first thing to repair is the harness rather than the inventory: whatever selector fills `observedPanelControls` matched nothing on 25 of the 26 rows that use it, which is a structured observation wired at one end and consumed at neither. **That harness repair is done, and the answer to “whatever selector fills it” turned out to be: none, there was never one.** A search of the whole tree finds `observedPanelControls` in the two guard scripts, in the 26 records themselves, and in one contract test, and in no script that writes one — the selector lived in an ad-hoc paste at a driving session and was never committed, so it could not be reviewed, tested, or fixed once. Twenty-five empty readings are what that looks like from outside. **Two properties of this application defeat the two obvious readers, and both fail silently.** The compiled shell, 6,277 lines and effectively the whole interface, declares zero roles and zero accessible-name attributes, and [**this clause said "no element anywhere in the renderer carries the dialog role" and was false — see the correction at the end of this line**] `drive.mjs` was counting dialogs through a reading that fed the flag deciding whether a click changed anything, leaving that flag blind to almost every outcome it needed to see; `gallery.mjs` printed “(a dialog was still open)” beside each shot on the same reading. And the shell renders 175 Material Symbols spans icon-first, so `textContent` on the regex builder's first tool button reads `backspaceDelete last` and every control name the driver recorded carried its glyph name glued to the front. The obvious fix is a recorded dead end: `gallery.mjs`'s ligature-prefix pattern needs a capital after the glyph name and so strips nothing from a lowercase label, and `checki · ignore case` is the builder's ordinary flag-chip state rather than a corner case. `scripts/ui-drive/observe-panel.mjs` now collects plain readings in the page and decides in Node where it can be tested: it finds the panel by stacking order (every literal z-index in the shell is at most 6 or at least 55, with nothing between), keeps a control named only by its icon OUT of `observedPanelControls` because a glyph name is a finding rather than a label, and records `panelFound: false` with the reason instead of the empty list that reads identically and means the opposite. Both drivers now share it, and `console/tests/scripts/panel-observation.test.mjs` re-derives every DOM fact from the shell on each run. **Twenty-one deliberate breaks, one at a time; nineteen went red immediately and two stayed green, and both of those were this pass's own guards** — a z-index check asserting `min(overlays) === OVERLAY_Z_FLOOR`, which is true of any floor landing on a real value and so passed while the floor was moved from 55 into the middle of the page chrome, and a comment stripper that checked only that stripping shortened the file, which dropping blank lines achieves on its own. Both repaired; all 21 now red one at a time and green on restore. **What this pass explicitly did NOT do, so the next one does not read it as more than it is: no application was run, no record changed, and no row moved to `verified`.** The position is still **4 of 88**. The reader exists and is proven correct on the readings it is given; driving a built artifact with it, and letting the 35 windows-console rows that already hold all six artifacts get real readings instead of empty ones, is the next step and needs a packaged build. Recorded in `console/docs/evidence/panel-observation.md`. **That packaged build was made and driven, and the first reading this harness has ever taken from a running program corrects the reasoning the harness was built on.** `scripts/ui-drive/palette-reading.mjs` — committed, so it can be reviewed and re-run, rather than the ad-hoc paste that started all of this — launches the packaged executable on an off-screen desktop under a throwaway profile, proves exactly one debugging target, and reads the command palette: the surface **all twenty-five** of the empty records were driven through. **The claim quoted above, that no element anywhere in the renderer carries the dialog role so a dialog-role count can only ever be zero, was false, and false about that exact surface.** The palette's card carries `role: 'dialog'` in `App.tsx`, it ships in the bundle, and the packaged application reports `dialogRoleElements` as 0 before the chord, **1** while the palette is up, and 0 again after a result is activated. The test guarding the claim could not see it: its needle was the JSX spelling `role="dialog"` and this renderer is hyperscript. Measured rather than argued — on a tree deliberately carrying the role in **two** places, the old needle still returned an empty carrier list. **And the previous pass had broken that guard on purpose and watched it go red**, which is the part worth carrying forward: the break was written in the spelling the needle expected, so it proved the needle fires and never that the needle was the right one. Break a guard in the form the code is actually written in. The replacement reads every attribute spelling this codebase uses, pins the exact set of carrier files, the exact count inside `App.tsx`, and the fact that it sits on `.palette-card`; a second test reads `.palette-scrim` and `.palette-card` out of `styles.css`, because the scan reaches the palette only through the scrim that wraps it — the card declares neither `position` nor `z-index`, so what gets chosen covers the viewport and can never read as anchored to anything. Nine deliberate breaks, one at a time, all nine red and all nine green on restore. **The deeper answer to why twenty-five lists were empty is not the missing selector alone.** Activating a palette result closes the palette *before* it teleports, so every one of those readings was taken when nothing was up: measured here as 11 controls with the palette open and 0 immediately after activation. The repaired reader writes `panelFound: false` with `whyNoPanel` at that moment instead of an empty list, which is the same fact stated so it cannot be mistaken for a panel that offered nothing — so the next pass knows the reading has to be taken twice, with the palette up and again once the destination has opened, rather than once after the click when neither is true. **A second defect of the ligature family, found only by looking at a real reading:** a control whose label and context are adjacent spans came back as one glued word, `languageHardware trunks · Signalling & routing`, because `textContent` puts nothing between element children. The browser side hands the top-level runs back separately now and `readControlLabel` joins them in Node, where it is a pure function with a test. Four further sentences that had never been checked are now measurements: `Ctrl+Shift+F` reaches the handler in the packaged build (a real key event, never the application's own toggle); the palette really filtered, its own hint reading `10 of 883`; it teleports rather than landing nearby, recording `focusedControlId: "da_language"` on the Hardware trunks screen; and the update banner was up during both captures and is recorded as `updateBanner` rather than described as dismissed. **What this pass explicitly did NOT do: not one of the 39 committed `release/evidence/windows-console/` records changed, and no row moved to `verified`. The position is still 4 of 88.** The reading lives apart from the per-feature records, in `console/release/evidence/ui-drive/command-palette-reading.json`, because it is evidence about the harness rather than about a feature. **Twenty-four routes remain to drive**, and the harness is now known-good on the twenty-fifth.**Those twenty-four are driven, and the twenty-fifth again with them, in one pass against one build.** `scripts/ui-drive/palette-routes.mjs` reads the route table out of the records themselves -- the query from each one's own `action` prose, the target from its `observedTarget` -- so a record that changes changes the table, a record that stops naming a query fails loudly rather than being driven against one it no longer claims, and the single record reached another way (`regex-builder`, from the dashboard section-search row) is named with its reason and asserted to still be there, because an allowance that excuses nothing is an error rather than a comment nobody removed. **The field that recorded an empty list twenty-five times now holds 266 controls, between 2 and 60 per route.** 25 of 25 opened the palette on a real `Ctrl+Shift+F` key event; every query was typed through the input domain rather than assigned, because the field is a controlled input and an assignment produces no event, so the list would never filter and no screenshot could tell the difference; and every route was entered from wherever the previous one landed, across ten different screens rather than being twenty-five readings of one starting state. **25 of 25 also report no panel after activation**, which is the prediction the previous pass made and is the exact fact those empty lists were a reading of. **The judgement was moved out of the driver, and that is the part worth having.** A reading saying "activating this row focused `e_displayname`" is worth nothing alone, because nothing in it says the row was supposed to reach that control. `buildPalette` gives a setting entry a `controlId` and a destination entry none, so `scripts/palette-route-readings.mjs` rebuilds the palette from the compiled design, finds the entry for the row that was activated, and compares: **22 setting activations each reached the exact control its entry names, and 3 destination activations focused nothing, which is what a destination entry names.** The driver observes and never classifies. **Three findings, none repaired here.** A palette row label does not always identify a result: `display name` returns two rows both labelled exactly `Display name`, separated only by their context (`Endpoints - Identity` and `Customise everything - Identity`), so `app-display-name.json`'s prose -- "clicked the 'Display name' result" -- does not say which, while its own `route` names the other one; matching by label reached the endpoint field and landed on `PJSIP endpoints`. It is reported with both contexts rather than resolved by a rule nobody argued for. Three records name their target in prose rather than as a row label and are recorded as `expectedTargetPresent: false` beside the row actually activated, because a substitution reported as a match is worth less than no reading. And the shared reader has always cut a panel's control list at 60: two routes stood on it, `browser-style-tabs` holding 63 controls and `material-appearance` 74, so the cut is the named `PANEL_CONTROL_CAP` now, the collector counts before it cuts, and a reading standing on the cap carries `controlListTruncated` beside the real number. **One correction to the previous reading's own provenance.** `command-palette-reading.json` carries a field called `commit`, documented in its driver as the commit the artifact was built from and produced by `git rev-parse HEAD`, which is the commit the *driver* ran from. Those coincided there, so nothing was wrong and nothing said the two were different things; here they came apart and the field would have said something false. It is `harnessCommit` now, and the artifact's own commit is a separate field that is not taken on trust: this run drove the same executable, so the two records must name the same digest and the run refuses if they do not, which makes the provenance a chain between two committed files rather than a sentence. `appSourcesChangedSince` names what moved in `console/app`, `console/shared` and `console/package.json` between those two commits -- two generated bundle files and nothing else -- and the checker re-derives that list with git rather than believing it, saying so in its own output where a clone does not hold both commits. **Twenty-four data breaks and five code breaks, each planted alone, each watched go red and then green on restore -- and two of the five stayed green the first time, both of them this pass's own guards.** Commenting out the collector's `operableControls` line left the whole suite green, because every test handed `summarisePanel` a panel object of its own making rather than one the collector built: producer unguarded, consumer well guarded, the same shape arriving from the producer side again. And the assertion that the cut uses the named cap read the *rendered* template, where a bare `60` and the interpolated constant are the same string -- a guard moving with the constant it guards, which this harness's z-index check had already been once. Both were repaired, and both then went red alone and green on restore. **What this does not claim, which is why it is still unticked.** No windows-console record was rewritten and no inventory row moved to `verified`: the position is unchanged at **4 of 88**. Nothing here operates a setting -- every route reveals and focuses a control and then leaves it alone -- so these readings deliberately do not satisfy `operated-interaction-evidence.mjs`, which is the gate the 35 already-photographed rows still have to pass, and driving those operations is the next step. The application was not rebuilt for this: it is the executable the previous reading was taken from, two generated bundle files behind this tree. And the 42 pages-site rows remain not an evidence job at all -- 23 of their 44 features are `absent` in `site/feature-registry.json` and 11 more `partial` -- so most of them need the feature built before anything can be photographed. `console/docs/evidence/palette-route-readings.md`. **This pass worked the other half of the item, and it is the half the line above keeps deferring: the 42 pages-site rows that need the feature built before anything can be photographed. One of the 23 `absent` ones is now genuinely built.** `app-display-name` -- the canonical contract that every user-facing page lets the person reading it rename what it calls itself -- is implemented on the published site, in `site/settings.html` (an IDENTITY card with a bounded field, a `Reset to shipped name` button and a live status line) and `site/app.js` (`applyDisplayName()`, called from `applyState()`). Two surfaces are renamed, named rather than swept: every `.brand-name` element -- the brand line in the header and footer of all six pages -- and `document.title`, recomposed each time from a `SHIPPED_TITLE` captured once at load so a second rename composes against the shipped title instead of the first rename's output, and a title that never carried the shipped name is left alone rather than guessed at. **The boundary is the feature, so it is what most of the proof is about.** A rename must not reach the `og:` metadata somebody else's chat window reads, the product prose that names the real software, or any identity constant -- `STORAGE_KEY`, `HISTORY_KEY`, the vocabulary and logo cache keys and the `ding-pbx-page-settings.json` export filename are literal constants nothing derives from the label, which is the failure that would otherwise orphan a visitor's stored settings the moment they renamed the page and which no amount of testing the rename itself would surface. **The contract test runs the real extracted source rather than asserting patterns over it**, in the style `complete-exports.test.mjs` established here, because "the value is stored" and "the setting persists" are both true of a rename that never reaches a pixel and a source-pattern test cannot tell those apart from a working one; its recording page keeps every selector asked for and every element id requested, so the boundary is proved by what the code reached for -- exactly one selector and exactly two ids -- rather than by what its comments claim. 26 tests, and `scripts/negative-display-name-site.mjs` plants **20 breaks one at a time**, each turning that file red and green again on restore, wired into `test:inventories`. **Three records this change made wrong were corrected rather than left**, which is most of what it turned up: the localization registry said `app-display-name` was `not-localized` with no COPY key, and the new card description ships four English and four Cantonese funny levels of which every one of the eight names the shipped product, so the row is `localized` and `displayNameDesc` is in `knownCopyKeys`; the `funny-levels` note claimed eight COPY keys and three markup hooks when the real figures were ten and three before this pass and are eleven and four after, so it had been two keys out of date already and its line numbers were removed rather than refreshed, since those are what went stale first; and `Reset settings` now clears the chosen name, so its confirmation text says so instead of silently skipping it. **Two of the twenty breaks did not land on their first run**, both because a `\n` anchor cannot match a CRLF file, and the script's own "did the bytes actually change" assertion reported them as failed cases rather than letting them read as guards that held -- which is the whole reason that assertion is there. **What ticking this would claim and does not.** The pages-site row for `app-display-name` stays `unverified`: the feature, its documentation, its localized copy and its local check now all exist, and the two artifacts that need a running program -- a built-artifact interaction record and a capture -- do not. The position on the 88-row inventory is unchanged at **4 of 88**. 22 site features remain `absent` and 11 `partial`. Nothing here drove the packaged console, so the 35 windows-console rows still waiting to be operated are exactly where the line above left them, and the artifact those readings were taken from is not in this tree, so the next pass driving them has to build one and re-establish the provenance chain that `palette-routes.mjs` currently proves by digest equality. Full suite green at `EXIT=0`, 3750 tests plus the site suite 13/13; `npx tsc -b` clean. `console/docs/platform/app-display-name.md`. **This pass built a second of the twenty-three `absent` pages-site features, and the two most useful things it produced were both corrections to its own guards rather than to the feature.** `dialog-emojis` -- the canonical "Show emojis in dialogs and message boxes" switch -- is now genuinely built on the published site, in `site/settings.html` (an APPEARANCE card with a visibly labelled checkbox, a funny-level description and a live status line), `site/app.js` (`state.dialogEmojis`, off by default so nobody who never touches it sees a change; `DIALOG_EMOJI_DECORATIONS` naming the six decorated surfaces -- the command palette, the regular-expression builder, the notification centre, the local history dialog, the reset-settings gate and the inline dismissal alertdialog inside the notification centre -- each with its own glyph; `MESSAGE_BOX_GLYPH` for the toasts, because a message box carries arbitrary text and choosing a glyph from that text would be inventing a meaning for it; `setDialogDecoration()` as the single writer; `applyDialogEmojis()` called from `applyState()`) and `site/styles.css`. **The boundary is the feature, so most of the proof is about it.** The canonical rule is blunt -- emoji never in a button, an action label, a field label, an accessible name or any other control text -- so the decoration is a separate element the code creates, inserted into the heading bar *beside* the `<h2>` each dialog is `aria-labelledby` rather than inside it, carrying `aria-hidden="true"` so it cannot enter an accessible name and `data-no-vocab` so the personal-vocabulary walker never caches a glyph as copy. Turning the switch off removes that element rather than emptying it, because an emptied span still occupies the heading row and still reads as an element to anything walking the document, so "off" would not be off. The contract test runs the real extracted source against a small recording DOM in the style `app-display-name.test.mjs` established, for the same reason: "the value is stored", "the checkbox reflects it" and "the setting persists" are all true of a switch that never reaches a pixel. **Two of this pass's own assertions were wrong on their first run, and both were found by running them rather than by reading them.** The byte-identical-copy comparison -- every string in the tree with the switch on against the same tree with it off -- reported a difference that turned out to be an artefact of a stub: `#notif-confirm` had been built with no children at all, so with the switch off its own empty `textContent` was collected as a leaf and with the switch on it was not. A stub with nothing in it cannot show whether a decoration displaced text, which is the one thing that comparison exists to see, so the fake now carries the paragraph and two buttons the real one does. And an assertion that each glyph is declared exactly once failed on `⚠️`, correctly: it is legitimately declared twice, by the reset gate and by the inline dismissal alertdialog. It is replaced by a scan asserting that **every** emoji-bearing line in `site/app.js` is a declared decoration -- which is the defect actually worth guarding, an emoji typed straight into a string of copy or a control label, and which no assertion about `setDialogDecoration` could ever have seen. That scan turned up one pre-existing exception and pins it rather than waving it through: the CLI-builder destination's `⌨` icon (U+2328, which is Extended_Pictographic), so a second emoji smuggled into a destination row fails there. **One planted break stayed green on its first run, and the break was wrong rather than the guard.** Dropping the `existing||` reuse from `setDialogDecoration` looked like it would make decorations stack; it does not, because the newly created element is then never inserted, so nothing accumulates and the suite is right to stay green. The replacement -- the code no longer recognising a decoration it placed itself -- turns both the stacking assertion and the removal assertion red, and the stacking assertion was separately confirmed non-vacuous by watching it fail alone. **A fourth finding came from the display-name script written in the previous pass, working exactly as intended.** Adding the new switch to the reset gate's list of things it clears moved a string that `negative-display-name-site.mjs` anchors on, and its own did-the-bytes-change check reported that as a FAILED CASE rather than letting a break that never landed read as a guard that held. Anchor updated. **Three records this change made wrong were corrected rather than left**: the funny-levels registry note said eleven COPY keys and four markup hooks where the real figures are now twelve and five; the funny-levels contract pins were moved to match, deliberately, since an exact pin that drifts is supposed to fail and be explained; and the localization registry moved `dialog-emojis` from `not-localized` to `localized` with `dialogEmojisDesc` added to `knownCopyKeys`, all eight of whose variants -- four English, four Cantonese -- state both facts the switch turns on, that the wording is unchanged and that no button, label or screen-reader name carries a glyph. A boundary stated at some funny levels and not at others is a boundary nobody can rely on. `scripts/negative-dialog-emojis-site.mjs` plants **28 breaks one at a time**, each turning the contract test red and green again on restore, and is wired into `test:inventories` where `tests/scripts/test-suites-are-wired.test.mjs` discovers it from the filesystem and would fail if it were not chained. **What ticking this would claim and does not.** The pages-site row for `dialog-emojis` stays `unverified`: implementation, documentation, localized copy and a local check now all exist, and the two artifacts that need a running program -- a built-artifact interaction record and a capture -- do not. The position on the 88-row inventory is unchanged at **4 of 88**. 21 site features remain `absent` and 11 `partial`. The desktop console was not touched, so `dialog-emojis` is still absent there and the article says so plainly rather than describing the site's switch as though the product had one. Nothing here drove the packaged console either, so the 35 windows-console rows waiting to be operated are exactly where the lines above left them. Full suite green at `EXIT=0`, 3775 tests plus the site suite 13/13; `npx tsc -b` clean. `console/docs/platform/dialog-emojis.md`. **This pass built a third of the `absent` pages-site features, and then shipped a defect that broke a real deploy, which is the more useful half of what it has to report.** `changelog-viewer` is now genuinely built on the published site, in `site/downloads.html` (a `CHANGELOG` section reachable from the page's own section tabs, holding a funny-level description, a collapsible search-and-date panel and the version list), `site/app.js`, `site/styles.css` and `site/build.mjs`. Every released version, newest first, its changes grouped by category, and the real commit behind each line. The data is real or absent: `scripts/bundle-changelog.mjs` already built the Markdown from this repository's own tags for the desktop console, so `site/build.mjs` reads the file that generator produced rather than asking git a second time -- the site and the console cannot end up carrying two changelogs from two different moments -- and the committed `site/app.js` ships both declarations empty so a page served from the source directory says so rather than carrying a stale copy. **Two properties carry the design.** The entries are a factual external record rather than this site's own copy: every other string here is restyled by the funny sliders and rewritten by a local vocabulary file, and a change summary is neither, so the list sits behind `data-no-vocab`, `changelogEntryMarkup` reaches neither `copyText` nor `applyVocabularyText`, and the sliders reach the description above the list and nothing inside it. And the commit link is the only part a reader can check for themselves, so `changelogCommitUrl` refuses anything that is not exactly 40 hex characters and refuses a repository that is not `https`. The rest is the ordinary site contract: search through the same `matchText` every other field uses so the anchored regex builder applies here too; two native date fields, which is what supplies the calendar's month and year jump and what makes a half-typed date detectable, since such a field reports an empty value and `validity.badInput`, so the code says so inline and never writes to the field; five range presets pinned equal to the set the code can compute; and an export that honours the active filter, offers every format the shared engine judges suitable, and states its own range on every row. **Then the defect, which is the part worth reading.** The build verified every referenced commit with `git cat-file --batch-check` before emitting a link, and reasoned: git answered, git said missing, therefore the commit is gone, therefore fail. That is false in exactly the case CI runs in -- `actions/checkout` clones ONE COMMIT deep, so `cat-file` correctly reported all 26 commits as absent *from that checkout*, and the Pages deploy died on a repository that has every one of them. `cat-file` alone cannot tell "the repository lost this" from "this clone never fetched it"; `git rev-parse --is-shallow-repository` can, so the judgement is a pure function now with its own tests -- nothing missing is `verified`, missing in a shallow clone or one whose depth git will not state is `unverifiable` and drops the links with the reason printed, missing in a complete clone is `dead` and fails -- and the Pages checkout takes `fetch-depth: 200` so the links actually ship. Both halves were verified against real clones rather than reasoned about: a real `--depth=1` clone reproduced the failure identically and then took the fixed build to exit 0, and a real `--depth=200` clone verified all 26 and emitted the links. **The lesson, because it generalises past this feature: 45 planted breaks had gone red alone and green on restore before this shipped, and not one saw it, because every one was about the SHAPE of the resolver -- is the call there, does it throw somewhere, does it return an empty repository on the fallback paths. All true, and all still true while the thing concluded something false. A guard that checks a function is called cannot check that what it concludes is true.** The negative script is **51 breaks** now, each red alone and green on restore, including the defect in both directions and the wiring break where the resolver stops asking about depth. Three of them were found by running it rather than written from a plan: two guards that could not see what they claimed to (an assertion that a control was wired, satisfied by the closure that merely *reads* that control's value, so deleting the search listener left everything green; and an assertion that the first render happens, satisfied by any handler that also calls it), and one break that degraded safely rather than breaking anything and was withdrawn with its reason recorded rather than kept for the count. Three neighbouring guards were corrected rather than worked around: `guided-forms` enumerates every `<select>` by hand and correctly caught the two new ones; `material-appearance` banned the word "preset" outright to pin the absence of an appearance-preset system, so the date-range block is excluded by name and every other mention still fails, which a narrowed needle would not have caught; and the funny-levels pins moved 12 to 13 keys and 5 to 6 markup hooks. One correction that was not this pass's to make is made anyway: the article said the DESKTOP changelog was "Not implemented", which had been false for some time -- `changelog.ts` and two wired tests were already there and `app/feature-registry.json` already said `partial` -- so it is fixed and attributed, since nothing here drove the desktop application. **What ticking this would claim and does not.** The pages-site row for `changelog-viewer` stays `unverified`: implementation, documentation, localized copy and a local check now all exist, and the two artifacts that need a running program do not. The position on the 88-row inventory is unchanged at **4 of 88**. 20 site features remain `absent` and 11 `partial`. Nothing here drove the packaged console, so the 35 windows-console rows waiting to be operated are exactly where the lines above left them. Full suite green at `EXIT=0`, **3813 tests, 0 fail**; `npx tsc -b` clean. Released as `ding-pbx-console-v0.0.271-r1`, non-draft, targeting `8b7252e57`, its 446 MB `Ding-PBX-Console-Setup.exe` answering HTTP 200; the fix landed as `15147e34a` with the Pages deploy green and the published `app.js` carrying the verified repository URL. `console/docs/platform/changelog-viewer.md`. **This pass built a fourth of the twenty-three `absent` pages-site features, and the two most useful things it produced were both corrections to its own guards rather than to the feature.** `narration` -- the canonical off-by-default spoken narrator -- is now genuinely built on the published site, in `site/settings.html` (an ACCESSIBILITY card holding the master switch, the narrated-language choice, a voice picker per language with its own live status line, and rate and pitch sliders), `site/app.js` and `site/styles.css`. It is off in `DEFAULTS`, so it is silent for anyone who never switches it on; `narrationGate()` is the single pure decision about whether a line is spoken, taking the current time as an argument rather than reading it, so every branch is decided by values a caller supplies; and `pumpNarration()` serializes the queue, so two utterances never overlap and "Both" means English and then Cantonese rather than both at once. **The first correction was to this repository's own documentation.** `docs/platform/narration.md` said a static documentation site "has no application events of the kind this feature narrates". That is false and had been for some time: the site has its own `notify()`, its own toast region and its own notification history, and thirteen call sites reach them. What it had no way to do was read any of them aloud. The article now says so, and says what was wrong rather than quietly replacing the sentence. **The half worth reading is the privacy boundary, because a narrator is the one surface here that can carry text off the computer.** A voice whose `localService` is `false` synthesises on somebody else's server, so the words handed to it leave this machine. Two rules follow and both are guarded. Narrated text never passes through `applyVocabularyText`: `narrationTextFor()` reads `copyLevel()`, the per-language copy *before* the personal vocabulary is substituted, so a private dictionary cannot be spoken through a remote voice. And a rejected personal-vocabulary file is the one rejection on the site whose reason is shown and never spoken, because several of those messages quote the file back -- a duplicate term, an over-long replacement -- while a rejected logo does speak its reason, since every reason that branch can carry is written by this site or by the browser's own file reader and none of them quotes the image. Both routes now go through one writer each (`rejectVocabulary`, `rejectLogo`) so the rule holds for every branch rather than for whichever one somebody remembered. Where the effective voice *is* network-backed the status line says so, in the automatic and fallback cases as well as the chosen one. **Three things it does better than the desktop console, each deliberate and none of them claimed as a repair to the console, which was not touched.** Its narrated lines carry their own English and Cantonese wording, so a Cantonese voice reads Cantonese words rather than English ones; a line the site has no Cantonese wording for is read in English rather than handed to a Cantonese voice, because that is not Cantonese narration but English mispronounced, and the status line under the unselected picker says that is what it does; and the Cantonese track ranks `yue-*` and `zh-HK` ahead of any other `zh-*`, since `zh-CN` is Mandarin and a Mandarin voice reading Cantonese is a different language rather than an accent. Two things it deliberately cannot do are stated on the card rather than implied away: a browser has no API that reports a running screen reader, so this narrator cannot step aside for one, and **Low stimulation** doubles as the site's reduced-sound setting, silencing it live -- errors included, because "quieter" that keeps talking is not quieter. **The second and third corrections were to this pass's own tests, and both were found by running the negative script rather than by reading it.** 48 breaks were planted one at a time; 46 went red and green on restore immediately, and two stayed green. The error-bypass flag turned out to be unobservable through the error category, whose own cooldown is 0 -- so the flag and the cooldown say the same thing there and no current call site distinguishes them; it is now asserted directly, under a category that does carry a cooldown, with that overlap recorded beside it. And the queue's outer drain guard turned out to be a **fast path** rather than the thing that stops the narrator: removing it leaves the queue emptying item by item while the per-line guard refuses each one, so nothing is spoken either way. The per-line guard is what actually stops it, and it is only individually observable between the two halves of one bilingual line -- which is now a test, after which that break goes red. Both breaks were rewritten to the property that is real rather than kept for the count. **Six neighbouring guards moved because this change moved their anchors, and three of those were reported by a sibling script's own did-the-bytes-change assertion rather than passing as guards that held.** `notify` gained a third argument -- the words to narrate, explicit at all thirteen call sites so a new notification cannot be silently mute -- which moved the signature anchors in `dialog-emojis` and `non-blocking-notifications`; the logo rejection moved behind `rejectLogo` so `app-logo-customization`'s bound anchor moved with it; `guided-forms` enumerates every `<select>` by hand and correctly caught all three new ones, with the two voice pickers joining `history-action-filter` in the real-per-visitor-data bucket, because nothing can know what voices are installed on a computer it has not asked; and the funny-level pins moved 13 to 14 keys and 6 to 7 markup hooks. One further guard was left alone on purpose: a comment of mine used the word "clock", which `per-element-toy-locks` scans for, so the comment was reworded rather than the guard's allowlist widened. **What ticking this would claim and does not.** The pages-site row for `narration` stays `unverified`: implementation, documentation, localized copy and a local check now all exist, and the two artifacts that need a running program -- a built-artifact interaction record and a capture -- do not. Nothing here has been driven in a browser: no real `speechSynthesis` has spoken a word of it and no real voice list has been enumerated, so it is proved against its own extracted source, a recording DOM and a fake speech engine, and no further. The position on the 88-row inventory is unchanged at **4 of 88**. 19 site features remain `absent` and 11 `partial`. The desktop console was not touched, so its `zh`-prefix voice matching and its single-language narrated text are exactly as they were. Nothing here drove the packaged console either, so the 35 windows-console rows waiting to be operated are exactly where the lines above left them. Full suite green at `EXIT=0`, **3871 tests, 0 fail**, against a `master` baseline of 3813 taken before anything was touched; `npx tsc -b` clean; `scripts/negative-narration-site.mjs` wired into `test:inventories`, where `tests/scripts/test-suites-are-wired.test.mjs` discovers it from the filesystem. `console/docs/platform/narration.md`. **This pass built the fifth of the pages-site's `absent` features, and it is the first one whose entire point is what it takes AWAY -- which turned out to be the difference between a switch and a feature.** `school-mode` -- the canonical restricted presentation -- is now genuinely built on the published site, in `site/settings.html`, `site/app.js` and `site/styles.css`. **What it does.** While it is on, four containers leave the document entirely: the language card with both funny selects, the personal-vocabulary card, the narrated-language choice and the Cantonese voice picker. Removed, not disabled and not hidden, because a disabled control is still a control somebody can see and ask about. Each is held in `schoolRetained` with an empty comment standing in its place, so turning the mode off puts back the same node, in the same position, with the handlers bound to it at load still attached -- and the settings behind them are never written while it is on, so a chosen language, a chosen funny level and an uploaded vocabulary file all survive the whole time. **Two of its effects are not containers at all, and that is where a mode like this usually ships half-built.** `copyText()` renders the plainest English variant, so the funny levels behave as though they were not installed rather than merely being unreachable; and `applyVocabularyText()` returns its input untouched, which matters precisely because the uploaded file is deliberately KEPT -- a mode that removed only the upload control would have gone on substituting from the cache behind it, with the control gone and the replacements still happening. **The value that lifts it is never stored**: what is written is a random 16-byte salt and the SHA-256 digest of salt-and-value, and a browser that gives the page no cryptographic digest at all (an insecure context such as a `file://` load) refuses to arm and says why, rather than keeping the value under a friendlier name. **Where the record lives is what makes the lock a lock.** It sits in `ding-pbx-pages-school-v1`, its own key beside the local history's, so `performSettingsReset()` and `restoreHistoryEntry()` -- both of which write `state` -- cannot reach it; a reset button that opened the mode would be a way around it rather than a reset, and the reset dialog now says outright that it is not. **The name rule has two halves and the second is the one that is easy to get wrong.** Live copy always renders the chosen name -- heading, status line, removal sentence, recovery text and the card's own search keywords are all written at run time -- while persisted text names the mode nowhere at all, not in a history entry and not in a stored notification, because history here is append-only and a rename cannot rewrite it: an entry written before a rename would sit in the record still naming what was just replaced, which for the first rename is exactly the shipped name the rename existed to remove. **Two deviations from the canon are recorded rather than left as silent gaps.** There is no attempt lockout and no waiting period, so this page can never lock anybody out on a clock -- which is why it ships no unlock ladder, there being no wait for one to shorten; wrong values are counted on screen and recorded in the local history instead. And the per-launch startup surprise cannot be suppressed here because this site has not got one, which is named in `SCHOOL_ABSENT_HERE` and re-derived from the real source by the contract test every run, so the day somebody builds one the pin stops being true and says so instead of the mode quietly failing to hide it. **One limitation is stated rather than hidden**: the card's heading and search keywords ship in `settings.html` carrying the default name and are overwritten by the first render, so a renamed card can show the shipped name for the moment between the markup parsing and the script running; shipping the heading empty would leave a blank heading for anybody whose script never runs, which is worse. **The two most useful things this pass produced were both corrections to its own assertions, and both were found by running the negative script rather than by reading it.** 56 breaks were planted one at a time; 54 went red and green on restore immediately, and two stayed green -- and both survivors were the same defect in the test rather than in the feature: an assertion whose needle was satisfied by an unrelated occurrence elsewhere in the same string. A bare `/digest/` against a registry note that mentions digests twice, and a bare storage-key needle against an article that names that key in four places. Both were tightened to the exact claim (`random 16-byte salt and the SHA-256 digest of salt-and-value`; the recovery sentence itself) rather than the breaks being softened. **Six neighbouring guards moved because this change moved their anchors, and one of them needed re-deriving rather than adjusting.** `per-element-toy-locks` pinned that feature's absence by banning the word "lock" outright, which this mode makes untrue -- so it now checks the property that actually separates the two (one credential, one fixed storage key, no per-element command and no context menu to offer one from) and keeps the word scan beside it as a cheap tripwire for a mechanism arriving under a third name. `funny-levels` moved 14 COPY keys to 15 and 7 markup hooks to 8. `destructive-action-confirmation` gained the sentence about what a reset deliberately does not clear. `personal-vocabulary-upload` follows its handler from `$(` to `el(`. And the narration harness gained two injected dependencies, four tests and two further planted breaks, because the narrator needs two guards rather than one: the queue-level filter never sees a line whose two halves are already inside one queued item, and the per-line guard never sees a line refused before it was queued. **What ticking this would claim and does not.** The pages-site row for `school-mode` stays `unverified`: implementation, documentation, localized copy and a local check now all exist, and the two artifacts that need a running program do not. Nothing here has been opened in a browser -- no real `crypto.subtle` in a page, no real `storage` event between two real tabs, no capture of the card -- so it is proved against its own extracted source over a recording DOM, a fake storage and Node's own Web Crypto, and no further. The position on the 88-row inventory is unchanged at **4 of 88**. 18 site features remain `absent` and 11 `partial`. The desktop console was not touched, so its own half of this feature -- the one whose `capabilityVisible()` and `effectiveLanguageMode()` are exported and called by nothing -- is exactly where the article already recorded it. Nothing here drove the packaged console either, so the 35 windows-console rows waiting to be operated are where the lines above left them. Full suite green at `EXIT=0`, **3923 tests, 0 fail**; `npx tsc -b` clean; site contracts 393 to 445; `scripts/negative-school-mode-site.mjs` wired into `test:inventories`, where `tests/scripts/test-suites-are-wired.test.mjs` discovers it from the filesystem. `console/docs/platform/school-mode.md`. **This pass built a sixth of the pages-site features that were `absent`, and the two most useful things it produced were both defects the tests found rather than repairs the plan anticipated.** `automatic-updates` is now genuinely built on the published site, in `site/app.js`, `site/build.mjs`, `site/settings.html` and `site/styles.css`. **A page installs nothing, so the canonical updater had to be read for what it is FOR rather than copied clause by clause**: notice that what is published has moved on, say so without interrupting anybody, and let the person take the new one when they choose. Reloading is the whole installation step. Four clauses have no equivalent at all -- no staged download, no signature, no restart, nothing to roll back -- and the settings card names all four out loud rather than implying machinery this surface does not have, because a silent gap reads as an oversight to the next person and as a decision to nobody. **The data is real or absent, on the same pattern the changelog already uses.** `site/build.mjs` resolves one build identity per deploy -- the commit from `git rev-parse HEAD`, the verified release label this site documents (or `unversioned`, since inventing a version number for an unreleased build would be a reading nobody took), and the build time -- bakes it into three constants in the published `app.js`, and publishes the same three as `dist/version.json`. A build that cannot name its own commit writes **neither**, so a page served straight out of the source directory reports itself unbuilt and never asks for a file nobody published; a request that fails reads as a site that is down, and an honest "this page was not built" does not. The committed source ships all three empty and a test pins that it does. **The comparison is on the COMMIT and never on the version label**, because two builds of one release wear the same label and a check resting on it reports `current` about a page that is not. The label is used for exactly one thing: `compareBuildVersions` orders two `v0.1.N` strings so the wording can say newer, older or rebuilt, and returns `null` for anything else -- guessing an order is how a roll-back gets announced to somebody as an update. **The one request this site makes cannot leave this origin, and that is the property worth being able to check rather than "it asked for the right file".** `versionManifestUrl` resolves `version.json` against the document and refuses any result whose origin is not the document's own; everything else here is a bundled local asset, and a hand-edited `data-base` would otherwise be enough to move it with nothing saying so. `cache: no-store`, `credentials: omit`, a 4096-byte bound applied **before** parsing so it holds whatever the body turns out to be, an eight-second deadline, and one check in flight at a time. **Three neighbouring guards banned `fetch(` outright as a proxy for something narrower and were right to go red**; each is now pinned to the property it actually rests on -- no external settings source, browsing the catalogue reaches no network, no authenticated connection to a live hub -- which is a stronger check than the ban was, since a blanket ban passes cleanly on a build where all of those had been reversed and the second fetch removed. **The two findings.** The already-announced check read `updateWatch` **after** the state had moved to `checking`, so it was always false and every half-hourly poll raised a fresh notification; the test caught it on its first run, and it reads the state held before the move now. And a settings reset clears the dismissal while `applyState` never re-rendered the banner, so a banner somebody had just been given back would have sat invisible for up to half an hour -- wired at one end and consumed at neither, this repository's oldest recurring defect, found by asking what `Object.assign(state, DEFAULTS)` actually does. The reset gate now names the dismissal among the things it clears. **50 breaks were planted one at a time**, each turning `site/tests/contracts/automatic-updates.test.mjs` red and green again on restore, including the commit-versus-label comparison, the origin refusal, the credentials, the deadline, the re-entry guard, the `Later` persistence, the reload control's own listener, the `applyState` wiring, the build inventing a commit it could not resolve, and the init call **commented out rather than deleted**, because that is how a wiring line usually dies and a bare substring needle is satisfied by the comment. One fixture was vacuous on its first run and is recorded rather than quietly fixed: the upper-case-commit refusal used `COMMIT_B.toUpperCase()` on a fixture of all digits, so it tested nothing at all. A sibling script earned its keep again -- `negative-changelog-site.mjs`'s own did-the-bytes-change assertion caught its `init` anchor moving under the new call and reported it as a FAILED CASE instead of passing as a guard that held. **What ticking this would claim and does not.** The pages-site row for `automatic-updates` stays `unverified`: the implementation, its documentation, its localized copy and its local check now all exist, and the two artifacts that need a running program do not. **Nothing here has been driven in a real browser: no `fetch` has been made by a browser, no deploy has been watched raising the banner on a loaded page, and no `version.json` has been served over HTTP.** It is proved against its own extracted source, a recording page and a fake network, and no further. The position on the 88-row inventory is unchanged at **4 of 88**. 18 site features remain `absent` and 11 `partial`. The desktop console was not touched, so its own updater is exactly where it was. Nothing here drove the packaged console either, so the 35 windows-console rows waiting to be operated are where the lines above left them. **One measured cost:** the published output is one file larger, 194 to 195, and it is the first output whose presence depends on the environment rather than on the source, so the exact count in `site.test.mjs` is now conditional on the build identity resolving rather than quietly widened to "at least". Full suite green at `EXIT=0`, **3954 tests, 0 fail**, on the merged tree after `master`'s School mode landed in the same four files; `npx tsc -b` clean; site contracts 393 to 476; `scripts/negative-automatic-updates-site.mjs` wired into `test:inventories`, where `tests/scripts/test-suites-are-wired.test.mjs` discovers it from the filesystem and was broken on purpose to watch it go red. `console/docs/platform/automatic-updates.md`. **2026-08-26: not advanced by this pass, and the reason is worth writing down rather than rediscovered.** Every remaining row's gap is the two artifacts that need a running program, and producing either means packaging the console afresh -- `package:squirrel` builds the Asterisk WSL bundle before it builds anything else -- and then driving that package headlessly, one row at a time. The measured position is unchanged at **4 of 88**: `regex-builder`, `unlock-ladder`, `built-in-authenticator` and `automatic-updates`, all on windows-console, with the pages-site surface carrying none. **35 of the 38 unverified windows-console rows already have both artifacts on disk**, which is the answer to the shortcut that suggests itself on first reading the directory: all 35 of those records name their own `notInterrogatedHere`, so each capture establishes that a section exists rather than that the contract holds, and flipping the rows would be a claim the evidence beside them does not carry. The three unverified rows with no artifacts at all are `browser-extension-download-surfaces`, `in-context-recovery` and `forge-publishing`. This pass took the next completable item instead.
- [x] **Fix the momentum-snooze test that asserts a zero-millisecond elapsed time.** `console/tests/ui/attention-modes-wired.test.tsx:323` asserts `msSinceSnooze(...) === 0` immediately after clicking "Not now", so it fails whenever the millisecond ticks over between the click and the assertion. Observed failing once on 2026-08-26 inside a full run with `1 !== 0`, and passing in the same tree when that one file was run alone, and passing in three other full runs of the same tree. It is a flake in the test rather than a defect in the product: the snooze WAS recorded, one millisecond before the assertion looked at the clock. A bound of a few milliseconds, or an injected clock, closes it. Left for its own pass rather than folded into an unrelated change, and recorded here rather than left to be rediscovered as a mystery red run. **Closed 2026-08-26.** The assertion now reads the clock immediately before and immediately after the click and requires the stamp to fall inside that window, so it is exact rather than tolerant: there is no millisecond budget anybody chose until the test went green, and the window is precisely the claim `recorded now` was always making. **Two breaks were planted one at a time**, each turning `console/tests/ui/attention-modes-wired.test.tsx` red and green again on restore: a stamp written five milliseconds stale, which reported `msSinceSnooze read 5ms against a 0ms click window`, and a stamp written as an empty string, which is the `undefined` half of the assertion and the half a purely upper-bound fix would have quietly dropped -- an upper bound alone passes cleanly on a click that records nothing at all. Both restores left `app/renderer/src/attention-modes.ts` byte-identical. **The product was not changed and never had a defect here:** the snooze was recorded correctly all along, one millisecond before the old assertion looked at the clock.
- [x] Make the completeness check resolve its evidence templates and require the artifacts to exist, so a row cannot claim `verified` without them, with a red-then-green negative regression proving it refuses false claims rather than refusing everything.
- [x] Download the design reference's complete 49-face font set from its own stylesheet URL, preserving every weight and `unicode-range`, replacing package substitutes that covered only a fraction of it.
- [x] Audit the design compiler attribute-by-attribute and confirm 100% of the design specification reaches the renderer; record that `design/support.js` is design-tool editor machinery rather than missing runtime.
- [x] Remove private vocabulary from every shipped and published surface, and add a hand-written-list guard with a planted-term negative regression so a new term cannot arrive unnoticed.
- [x] Create, verify, stop and remove the console's own WSL distribution from the packaged Asterisk payload, replacing a `server.provision-bundled` action that was declared and implemented nowhere.
- [x] Make the confirmation flow dispatch the command it guards and report the real outcome, including refusals, instead of announcing success without calling anything.

## Cover Asterisk's real capability surface

A survey against this checkout measured the console at roughly **12%** of Asterisk's
configurable surface: 106 shippable sample configuration files, about 13 the console
could name. The owner's direction is that every gap below is closed, partials included.

**Foundation — done.** The control plane no longer limits this: writable resources went
from 10 files to 41 and read-only commands from 23 to 63, every name verified against the
Asterisk sources by a test that refuses an invented one. A new destination needs no change
to the control plane.

### Major gaps — no destination exists at all

- [x] **Access control rules** (`acl.conf`) — the highest-value gap by far. SIP scanning is constant and toll fraud is the expensive failure; the security screen currently edits no rule at all Shipped as a table of every `permit=`/`deny=` rule across every named ACL in file evaluation order, with add, edit, remove and reorder writing through the existing plan-and-apply path. Deliberately not bound through the single-key control table: that table is permit-only and would have silently dropped any interleaved `deny=` line.
- [x] **Sound prompt management** — upload, list, audition and remove prompts. Every IVR and voicemail screen references prompts it cannot create Shipped as a Sound prompts destination listing every real file on the target, with upload through a native picker, removal behind the destructive-action gate, and audition through a new media read action. Audition refuses the five raw telephony formats outright rather than failing silently: they carry no header, sample rate or framing a browser could recover.
- [x] **TLS and certificate management** (`http.conf` TLS, PJSIP transport certificates, STIR/SHAKEN keys) — every other screen assumes certificates that nothing can install or rotate Shipped as a TLS group that loads a named PJSIP transport and writes ten fields back, a STIR/SHAKEN key-material group, and the first write path http.conf ever had -- its bindings were complete and unreachable. Paths only, never key contents. Saving refuses outright when the typed transport does not resolve, so it cannot invent a half-built transport carrying TLS keys and no bind address.
- [x] **Hardware trunks** (`chan_dahdi.conf`) — analogue lines, T1/E1 and PRI. Landed: a Hardware trunks destination editing `[channels]`'s own cumulative signalling/caller-ID/echo-cancellation/gain defaults (twelve fields, every one bound to a line in the shipped sample), plus add/remove for the repeated `channel =>` span directives those defaults apply to -- a shape no single-key binding could carry, so it reads and writes the parsed section directly, the same way the access-control-rules editor already does for `permit=`/`deny=`.
- [x] **Database backends and realtime** (`res_odbc.conf`, `extconfig.conf`, `sorcery.conf`, `res_pgsql.conf`, `res_ldap.conf`) — required for hosted and multi-tenant deployments. Landed: a destination for res_odbc.conf, extconfig.conf and sorcery.conf, with a mapping model of its own and every control bound to a line in the shipped sample.
- [x] **Fax** (`res_fax.conf`, `udptl.conf`) — sending, receiving, T.38 gateway. Landed: a Fax & T.38 destination writing res_fax.conf through the transaction.
- [x] **Channel event logging** (`cel.conf`, `cel_odbc.conf`, `cel_pgsql.conf`) — the compliance counterpart to call records. Landed: a CEL panel on the CDR & CEL destination with its own save action writing cel.conf, plus cel_odbc.conf and cel_pgsql.conf handlers, every control bound to a line in the shipped sample. All three go through the backup/stage/validate/apply/post-read transaction.
- [x] **Call attestation** (`stir_shaken.conf`) — profiles, certificates, verification. Landed: a Call attestation destination for stir_shaken.conf's own `[profile]` objects, distinct from the Security screen's `[attestation]`/`[verification]` singletons -- a named profile, its endpoint behaviour and its overrides of level, signing key/certificate and x5u ACL, six controls bound to lines in the shipped sample plus `type=profile` written on save. TN objects are out of scope; the screen's own description says so.
- [x] **Emergency-services location** (`geolocation.conf`) — a regulatory requirement in several jurisdictions. Landed: an Emergency-services location destination covering both object shapes the file has -- a named `[location]` (format, info, method, source) and a named `[profile]` (precedence, PIDF element, referenced location, routing use) -- eight controls, every one cited against the shipped sample, with `type=location`/`type=profile` written on save.
- [x] **Handset auto-provisioning** (`phoneprov.conf`) — the deployment flow asks how many phones and has nowhere to template them. Landed: a Phone provisioning destination writing the `[general]` section's `default_profile` (what the wizard's own "How many phones?" question had nothing to feed) plus a named provisioning profile's static-file directory and MIME type. The profile's actual file list is dozens of entries in a real deployment and is edited on the target directly -- a single field here could show only one at a time, which the screen's own description says plainly is worse than no field.
- [x] **Feature codes and parking** (`features.conf`) — transfer, park, pickup, recording keys Thirty controls bound in total: fourteen against `features.conf` for transfer, pickup and recording, and sixteen against `res_parking.conf`, which is where parking actually lives -- `features.conf.sample` says so in its own fifth line, and its `[featuremap]` carries only the park trigger. Every one cites its sample line. 2026-08-26: reading was real from the start (`CONTROL_BINDINGS.fcodes` seeds the screen generically) but there was never a Save button on the screen at all, on any group, ever -- confirmed by grep. Fixed with two Save actions: `fc_save` (`fcodes-save`) writes the fourteen `features.conf` controls through `feature-codes.ts`'s own `applyControlValues`/`featuresDocument`, which preserves `[featuremap]`'s `name => sequence` list shape rather than treating it as an ordinary key/value section; `fc_park_save` (`fcodes-parking-save`) writes the sixteen `res_parking.conf` controls through the generic bound-control path, read as its own file the same way the Fax screen's udptl.conf already is.
- [x] **Endpoint caller ID** (`pjsip.conf`) — an extension's display name and outbound caller ID number had no fields anywhere on the Endpoints screen; `extensions.ts` (`parseCallerId`/`callerIdFor`/`formatCallerId`) existed, complete and tested, with nothing on screen ever calling it. 2026-08-26: two text fields (`e_displayname`, `e_calleridnum`) added to the Identity group; `endpoint-edit.ts`'s `controlValuesFor`/`applyControlValues` now import and use `parseCallerId`/`callerIdFor` to seed and compose the one `callerid=` key from the two fields, gated so an untouched pair never overwrites an existing value with nothing.
- [x] **Advanced trunk settings** (`pjsip.conf`) — `trunk-advanced.ts` existed, complete and tested (send_connected_line, contact_user, from_domain, from_user, media_address, the T.38 group, and outbound identity trust/RPID/Diversion -- thirteen controls total), with no group on the Trunks screen and no Save action at all. 2026-08-26: three new groups (Advanced, T.38 fax, Identity trust & routing) added to the Trunks screen with all thirteen controls plus a Save button (`tk_save`, action `trunk-advanced-save`); picking a trunk row now seeds these alongside the existing endpoint controls, since a PJSIP trunk is the same pjsip.conf endpoint `endpoint-edit.ts` already writes. Saving surfaces `trunk-advanced.ts`'s own post-write warnings (T.38 keys set while T.38 is off; Remote-Party-ID sent without trusted outbound identity) after a real write, never instead of one.
- [x] **Shared line appearances** (`sla.conf`). Landed: a Shared line appearances destination with a Trunk group and a Station group, each a named `[section]` picked and loaded by name the same way `res_odbc.conf`'s connections already are, plus add/remove for a station's own repeated `trunk=` assignment list.
- [x] **IAX2 trunking** (`iax.conf`) — currently only illustrative rows, which is worse than absence because it reads as configurable. Landed: an IAX peers destination writing iax.conf through the transaction, replacing the illustrative rows.
- [x] **Configuration backup, restore and diff** across the whole tree — no safe whole-config recovery exists. Landed: a Configuration backups destination reading `ConfigHistory#list` across every configurable resource at once (the same timestamped backups every write already took, with no renderer surface before this), with per-row Restore (`ConfigHistory#restore`, already existed) and a new Diff action -- `ConfigHistory#diff` is a real Myers line diff against whatever is on the target right now, plus a retention Prune action wired onto the existing `ConfigHistory#prune` that had no caller until now. `history.diff`/`history.prune` added to the dispatcher alongside the existing `history.list`/`history.restore`. Note: an older fallback admin surface (`PbxAdminApp.tsx`) already exposed list/restore behind the destructive-action confirmation; this closes the gap on the main M3-compiled console every operator actually opens, and adds the diff that surface never had either.
- [x] **A live REST resource browser** — channels, bridges, applications, events, beyond a static table. Landed: a REST resources destination combining five live readings into one table -- channels (already read elsewhere), bridges (new `bridge show all` parser, verified against a live target: 0 rows, honestly documented as bridges existing only while channels are actually mixed), registered dialplan applications (new `core show applications` parser, verified against a live target: 178 real applications), and what ARI itself reports as its own registered Stasis applications and configured users (`ari show apps` already read elsewhere; `ari show users` was allowlisted and never read by anything until now). Left honestly unclosed: a live *event* stream -- watching a channel or bridge form while the screen stays open needs a websocket/SSE transport this control plane does not have, the same gap the Manager and REST screen already states for itself.
- [x] **Dialplan scripting visibility** (AGI). Landed: a Dialplan scripting destination cross-checking every `AGI()`/`EAGI()`/`DeadAGI()` call `dialplan show` reports against what the target's own AGI directory (`asterisk.conf`'s `astagidir`, read live rather than assumed) actually holds -- a referenced script that is missing, a file sitting in the directory nothing calls, and a FastAGI URL or `agi:async` correctly shown as neither missing nor stray, since there is no local file for either to be checked against. Read-only visibility, as the roadmap line names it; editing a call still happens on the Dialplan canvas.
- [x] **Distributed dialplan lookup** (`dundi.conf`). Landed: a Distributed lookup destination with the `[general]` contact/network/query/outgoing-call fields (seventeen, every one bound), a named-peer group picked by entityid, and a mapping editor over `[mappings]` -- whose key rather than section varies per entry, so it reads and writes through `findEntry`/`writeEntry` the same way `extconfig.conf`'s family mappings already do.
- [x] **Calendars** (`calendar.conf`). Landed: a Calendars destination, every calendar its own named `[section]` picked by name; the account password is write-only, taken and blanked the instant Save reads it, exactly like every other credential field in this console.
- [x] **Monitoring integration** (`res_snmp.conf`, `prometheus.conf`) — a Monitoring destination on the Data rail. res_snmp.conf's own two [general] keys (subagent, enabled) and five of prometheus.conf's (enabled, core_metrics_enabled, uri, auth_username, auth_realm) are bound and round-trip through separate reads/Save actions per file; the Basic Auth password is write-only, the same take-then-blank shape res_pgsql.conf's own password already uses on the Database backends screen.
- [x] **Directory and identity settings** (`asterisk.conf`), NAT discovery (`res_stun_monitor.conf`), messaging (`xmpp.conf`), caller display (`adsi.conf`) — four new destinations. Directories & identity binds all twelve `[directories]` paths plus seven `[options]` identity fields and four capacity limits from asterisk.conf. NAT discovery binds both res_stun_monitor.conf keys. Messaging binds xmpp.conf's six `[general]` behaviour switches; the credential-bearing `[asterisk]` connection section is deliberately left unbound. Caller display binds adsi.conf's `alignment`; the repeated `greeting =>` welcome-message lines are deliberately left unbound -- this console has no control shape for an ordered, unlimited, free-text list, and the screen says so.

### Partial gaps — a destination exists but covers a fraction

- [x] **Security** — edits no access rule and no attestation certificate despite naming both. Both were already false by the time this line was picked up: the ACL table (add/edit/remove/reorder a real `permit=`/`deny=` rule) landed with the access-control-rules lane, and the attestation certificate (stir_shaken.conf's `private_key_file`/`public_cert_url`/CA material) landed with the TLS-and-certificate-management lane -- this bullet was simply never ticked. Verified both are genuinely wired (not just present in the design) and added `console/tests/ui/voicemail-ami-deepen.test.tsx` as a regression guard, since nothing previously pinned either fact.
- [x] **Trunks** — PJSIP only; no IAX2, no hardware, no registration retry detail. IAX2 and hardware trunks were already closed by separate destinations (lines above); the remaining gap was retry detail, and it was decorative -- t_retry/t_forbidden/t_fatal/t_pai/t_100rel were seeded from whichever `[registration]`/`[endpoint]` happened to be first in pjsip.conf and nothing anywhere wrote them back. Landed: `trunk-registration.ts` (a named `[registration]` lookup pjsip.conf's own type=registration disambiguates from a same-named endpoint, since `parsePjsip` deliberately excludes that object type) plus `App.tsx`'s `onPickTrunkRow`/`onSaveTrunk`, so clicking a PJSIP row in the live table loads and saves that exact registration and its paired endpoint. Also wired `trunk-advanced.ts` -- a fully built, fully tested module with thirteen `tk_*` controls (T.38, contact/from/media identity, trust and diversion headers) that had sat unimported by anything, its own control ids never once appearing in the design -- into a new "Advanced" group, and gave `t_pai`/`t_100rel` a real load/save path for the first time alongside it.
- [x] **IVR** — no dialplan application depth, and prompts are names it cannot manage. Landed: a real key map (`i_keydigit`/`i_keydest`/`i_keytarget`, add/remove/status) that routes a pressed digit to an extension, a queue, a voicemail, the operator, a hangup or a repeat -- previously the table's own "Keys" column claimed a count of routed digits and the generator routed none of them, the only route out besides the invalid-entry fallback was direct-dialling an arbitrary extension. And a real `i_prompt` field bound to the same prompt library the Sounds screen manages, with its own Audition button reusing `onAuditionPromptRow`, so a menu's greeting can be verified before it is written into a dialplan rather than being a name typed and hoped for.
- [x] **Logger** — level chips only; no rotation, no queue log, no per-channel configuration.
      **Closed.** The screen had five bound controls (`g_console`, `g_verbose`, `g_file`,
      `g_rotate`, `g_queue`) and no Save button of any kind, so "level chips only" was
      true in the strongest sense: nothing on this screen had ever been written, rotation
      and queue log included. It now has two Save buttons -- one for logger.conf's own
      four fields (console levels, file levels, rotation strategy, queue logging), and a
      separate one for verbosity, which lives in asterisk.conf's `[options]` `verbose`
      key, not in logger.conf at all. A third group, "Named log channels", closes
      per-channel configuration: `[logfiles]` in logger.conf holds one line per channel
      (`console`, `messages.log`, and anything else an admin adds -- `full.log`,
      `debug.log`, `syslog.local0`), so a channel-name field plus a level chips field,
      with Load/Save actions, edits any one of them by name without touching the others.
      Also fixed along the way: `g_file` was bound to the key `messages`, which does not
      exist in logger.conf.sample -- the real key is the literal channel name
      `messages.log`, dot included (sample line 176). Nobody had noticed because nothing
      had ever tried to write it.
- [x] **Modules** — no per-module reload and no dependency view.
      **The "no per-module reload" half is now genuinely closed.** The IPC surface a
      previous pass correctly said did not exist has been built: `control-plane/
      write-commands.ts` is a second, deliberately tiny allowlist beside
      `READ_ONLY_COMMANDS`, validating and building the exact three CLI lines
      `main/cli.c`'s `handle_load`/`handle_unload`/`handle_reload` accept (`module load
      <name>`, `module unload <name>`, `module reload <name>`), and
      `LocalAsteriskCliGateway.runUnchecked` sends whichever one a caller already
      validated as one `argv` element, no shell, so a rejected module name cannot
      smuggle a second command in behind it. `dispatch.ts`'s `pbx.command` action now
      accepts these two shapes alongside the read-only allowlist -- the same route the
      confirmation ceremony already used for read-only CLI runs, so it needed no new
      renderer plumbing, only a wider gate. Every row in the Modules table now offers
      real Load/Unload/Reload from its own context menu, gated on the row's own live
      State cell (a "Not loaded" row offers Load; anything else offers Unload/Reload),
      and a successful action drops the cached `modules` reading so the very next
      refresh re-reads the target rather than showing stale state. **The "dependency
      view" half is closed as honestly as it can be, which is not a graph.** Asterisk's
      own CLI genuinely has no command listing a module's runtime dependents or
      dependencies -- `module show` prints name/description/use-count/status and
      nothing else -- so "Show declared policy for …" on the row menu shows the one real,
      sourced relationship this console has: whether modules.conf itself lists that
      module in `preload=`/`noload=`/`require=`/`load=`, read back from the same bound
      fields the Load-policy group already writes. It says plainly, in its own text,
      that this is not a dependency graph and that Asterisk exposes none. Also still
      true from the earlier pass: the screen's four fields now have a real Save button,
      `mo_load` is bound to the sample's own `load =>` key, and `mo_preload`/
      `mo_noload`/`mo_require`/`mo_load` all use `repeated: true`, one line per module,
      rather than the comma-joined/boolean shapes that would have written a line
      Asterisk reads as one nonexistent module. Verified with a new dedicated test file
      (`tests/control-plane/write-commands.test.ts`, 7 tests covering command-injection
      and malformed-name refusal) plus 15 source-anchored tests in
      `tests/ui/modules-ami-codecs-deepen.test.tsx`; every one was broken on purpose,
      watched red, and restored.
- [ ] **Call records** — one status reading; no backend selection across the several available.
- [ ] **Manager and REST** — the operable actions landed; the live event stream did not. `ami-kick-session`, `ami-connected-status`, `ami-manager-save` and `ami-http-save` are real and reach real handlers, so the table is no longer inert. What remains is the stream itself: the design names one in three places and App.tsx implements none, so the screen still cannot show an operator what the manager interface is doing right now. Narrowed from the original wording, which claimed the whole screen was static and is no longer true.
      **The "no operable actions" half is now genuinely closed; the "no live event
      stream" half is still correctly left open.** `manager show connected`
      (`main/manager.c` `handle_showmanconn`) was already read-only-allowlisted but had
      no parser and no reading wired to it -- the AMI & REST table only ever showed the
      *configured* manager.conf/ari.conf users, never who is actually connected right
      now. It has one now (`parseManagerConnections`, `readings.ts`
      `managerConnectionsStatus`), wired into the `ami` view alongside the existing
      settings/users/apps reads, and a live "Connected sessions" group on the screen
      shows it. Beside it, "Kick session" runs the one real per-session action this
      interface has -- `manager kick session <file descriptor>`
      (`main/manager.c` `handle_kickmanconn`) -- through the same write-commands.ts
      allowlist and confirmation ceremony the Modules row actions above use, ending
      that AMI/HTTP socket for real. Left unticked because the live *event* stream
      (watching a session connect, or an AMI event fire, while the screen stays open)
      genuinely needs a websocket/SSE transport this control plane does not have; the
      screen's own copy now says so plainly next to the sessions list, which only ever
      reflects the moment it was last read. What was already fixed by the earlier pass
      (the "static table" half -- the compound `file` label, the two wrong-file
      bindings, the real Add/Remove API user flow) is untouched by this pass.
- [x] **Voicemail** — no storage backend configuration and no greeting management. This screen had ten already-bound fields and no write path of any kind -- no Save button existed on any group. Added three: mailbox defaults (the original ten fields), a Storage backend group (ODBC connection/table/on-disk-audio, IMAP greetings/folder/server/port -- seven fields, every one cited to configs/samples/voicemail.conf.sample), and a Greeting management group (maxgreet, forcegreetings, tempgreetwarn -- three fields). Per-mailbox greeting *audio* upload/replace remains undone: it would need a new media-library root pointed at each mailbox's own spool path, which the sample does not document a stable location for, so it was left rather than guessed.
- [x] **Codecs** — per-endpoint negotiation landed. `codecs-endpoint-lookup` and `codecs-endpoint-status` reach a real handler that validates the pjsip.conf endpoint id, refuses plainly when the field is empty or the id is unusable, and checks the target is connected before asking it anything -- rather than showing a plausible answer for an endpoint nobody verified exists.
      **A real, read-only per-endpoint negotiation lookup is now implemented; editing a
      single endpoint's own codec preference remains genuinely out of scope, as the
      earlier pass correctly said.** The screen's global translation graph
      (`core show translation`/`core show codecs`) names no endpoint at all, so a new
      "Per-endpoint negotiation" group looks one up on demand: `pjsip show endpoint
      <id>` (`res/res_pjsip/pjsip_configuration.c`), the only CLI output that prints one
      endpoint's own configured `allow=` codec list and `transport=`, run directly
      through the allowlisted `pbx.command` route rather than behind the confirmation
      ceremony, since it is read-only. Reaching it required fixing a real gap in
      `dispatch.ts`: `pbx.command` checked `READ_ONLY_COMMANDS.includes(command)`
      directly, which can never match an *object* command like `pjsip show endpoint
      <id>` -- `isAllowedCommandLine` in `asterisk-readings.ts` already covered that
      shape and was simply never called from here, so no screen driving this route
      could ever look up a single endpoint. **Editing** one endpoint's own
      `allow=`/`disallow=` preference in pjsip.conf -- the interpretation the earlier
      note correctly sized as a materially larger, separate feature -- is still not
      implemented, and this pass does not claim otherwise. The earlier fix (the real
      `file: 'rtp.conf'`, the asterisk.conf transcoding split, the two Save buttons) is
      untouched.

### How each one lands

Each destination needs: its resources in the writable list (**done for all of the above**),
its read-only commands (**done**), a screen, and control bindings mapping each control to a
real Asterisk key. The first two are finished, so the remaining work per subsystem is the
screen and its bindings.

**One honest constraint that governs all of it:** nothing here has yet written to a live
Asterisk beyond a disposable target. No configuration mutation may be described as verified
until an approved plan has run against a real exchange.

## Build the Asterisk runtime image in CI instead of on every machine

The root filesystem is produced by building a container image that compiles Asterisk from
this checkout. That takes tens of minutes, needs a working local container engine, and —
until the tar-listing defect was fixed — had never once succeeded on a developer machine,
which is why no published installer has ever carried the payload.

- [x] Build the runtime image in the release workflow and publish it to the registry, tagged and digest-pinned by the exact source commit. Done, and verified against the registry rather than against the record that claims it: the anonymous GHCR API lists the tag `83fdd85be478ac797fcbea95342e14c6c68af40a` -- the exact source commit -- and the digest the shipped provenance names resolves to a manifest with HTTP 200. No credential was needed for either, which is also the proof the package is genuinely public.
- [x] Have the packaging step pull that image and export the root filesystem, rather than compiling Asterisk again. The application still ships the exported tar inside the installer, so nothing is pulled at run time on a user's machine and the offline guarantee is unchanged. Done. `package:squirrel` runs `bundle:asterisk`, which is `build-asterisk-wsl-bundle-from-image.ps1`: it reuses a valid bundle for the exact commit, otherwise pulls the commit-pinned image and exports its root filesystem, and only compiles when neither is available. The Windows packaging job downloads the Linux job's artifact and so takes the first path, so Asterisk is compiled once per commit rather than twice.
- [x] Record the image digest in the existing provenance file alongside the source commit and the tar's own hash, so a shipped payload can be traced to the exact image that produced it. Done. The shipped `asterisk-wsl-rootfs.json` carries `sourceCommit`, `imageRef`, `imageDigest` and the tar's own `sha256` together, so a payload on a user's machine traces to the exact image that produced it. Read back off the published release asset, not from the script that writes it.
- [ ] Keep the local build script working as the fallback path and prove it from a cold checkout, so a contributor without registry access is never blocked.

Expected effect: the packaging step becomes a pull and an export rather than a compile, the
payload becomes reproducible from a digest rather than from whatever a machine happened to
build, and the local toolchain stops being a prerequisite for producing a release.

## Wiring the interface to real behaviour

Position now: **16 of 32 destinations are backed by live data** and **21 control-plane
actions are implemented**. When this work began it was 7 and 3.

- [x] Implement the configuration transport `ConfigTransaction` and `StructuredConfigPlanner` require. Allowlisted absolute resource paths, content on standard input rather than in an argument, ordered entries so repeated keys survive a round trip, and a post-read mismatch that rolls back instead of reporting success.
- [x] Wire `pbx.config`, `pbx.plan` and `pbx.apply` so a change previews as a real diff against the target and commits through that path.
- [x] Read the real configuration file on every screen that declares one, and seed the bound controls from it, so a switch shows the target's setting instead of a shipped default.
- [x] Bind controls to real Asterisk keys, each justified by a line in Asterisk's own samples. Was 82 of 130 across 13 screens when this line was first ticked; measured now (`console/tests/ui/telephony-coverage.test.tsx`) at **511 of 512 across every screen this console has**. The one exception is `ad_greeting` on Caller display (adsi.conf): a repeated bare `greeting =>` line per welcome-message line, and this console has no control shape for an ordered, unlimited, free-text list -- the screen says so rather than guessing at a binding that would need inventing one. A wrong binding writes the wrong setting to an exchange and looks like it worked, which is why every one of the 511 is justified by an exact sample-file line rather than a plausible-looking guess.
- [x] Implement `history.list` and `history.restore` against the backups that path already takes.
- [x] Give ten previously unreadable destinations a real reader: voicemail, conferences, music on hold, codecs, access control, call records, logging, manager and REST, and the two system screens. Fourteen parsers, each shaped from the literal format string in Asterisk's own source rather than a guess — a guessed parser returns an empty list, which is what those screens already showed, so the defect would have been invisible.
- [x] Add media management so a screen offering a custom prompt can accept a file, refusing by name before a command is built and confirming what landed.
- [x] Add a real append-only local history where a restore is a new record rather than a rewrite.
- [x] **Exercise an approved write plan end to end against a running Asterisk.** The console provisioned
      its own distribution from the bundled root filesystem, digest verified against the recorded
      provenance, and a plan ran backup, stage, validate, apply and post-read through the real
      `StructuredConfigPlanner` and `ConfigTransaction`. The Asterisk command line -- a channel independent
      of the transport that performed the write -- reported the new dialplan context before, during and
      after, and the exchange was restored to its exact prior state. Evidence:
      `console/release/evidence/live-exchange/write-plan.json`.
- [ ] **Repeat that write against a non-disposable exchange.** The run above proved the path against an
      Asterisk the console created for the purpose, which is still one it may safely destroy. A write to an
      exchange somebody depends on needs a target and an authorization only the repository owner can
      supply, so this is annotated rather than attempted.
- [x] **Stop corrupting `key =>` lines on every write.** Reading a resource and writing the identical value Fixed: the transport now renders over the original text rather than re-emitting a parse, keeping comments, blank lines and the arrow separator verbatim; only changed entries are re-rendered. It also reads with base64 rather than cat, because the executor redacts stdout and a redacted read written back replaces a real credential with the word that hides it -- measured at 29 bytes lost on this project's own sample dialplan.
      back rewrites `exten => 8100` as `exten = > 8100`, which Asterisk parses as an extension literally
      named `>8100`. An unchanged round trip of `extensions.conf` took it from 61 `exten =>` lines to 0 and
      changed all 161 dialplan and include lines. Thirteen shipped sample files use the arrow form, 419
      lines in total. `validate` and `post-read` both compare the parsed structure, which round-trips
      consistently, so neither can see it -- the transaction reported "Configuration applied and verified".
- [x] **Return the backup handle from `ApplyResult`.** The transaction takes a backup and completes its Fixed: a successful apply hands back one backup handle per changed resource, so an undo needs no second backup and one edit leaves one copy on the target.
      `backup:` action, but the result exposes no handle, so a caller can only undo a *failed* apply. A
      deliberate undo after a successful one has no supported route.
- [x] Bind the remaining controls, each from a key justified in the samples, or state on the screen exactly which setting it cannot write. None guessed. 511 of 512 now bind to a real key (see the tick above); the sole remaining gap, `ad_greeting`, states on its own screen that it cannot write adsi.conf's repeated `greeting =>` lines rather than inventing a shape for them. `console/docs/platform/unbound-controls.md` records the earlier `orphan-controls.test.mjs` figure (13 controls removed rather than bound, two of which -- Server certificate and Verify client certificates -- have since come back with real keys) with the reason for each remaining one.
- [x] Call the media, local-history and runtime actions from their screens. Done in `afe114fce`: `runtime.stop`/`runtime.remove` reach a real "Runtime maintenance" group on Deploy & servers, gated by `canStopRuntime`/`canRecoverRuntime` and a destructive-action confirmation for removal; `local-history.list`/`.record`/`.restore` reach a real Local history screen with two genuine record call sites. `media.*`, `history.*`, `runtime.status` and `runtime.provision` turned out to already be reached (PbxAdminApp and the onboarding wizard) once actually grepped for, rather than assumed unreached from this note's own wording.
- [x] Remove or gate `server.connect`. Also settled in `afe114fce`: it is already called from `App.discover`, confirmed by grep rather than by this note's own claim that it was unused -- so it stays, with a regression guard (`action-wiring.test.tsx`) proving the real call site.

### Platform contract features delivered

Each is a logic layer with its own tests, built to be driven by a surface rather than to
be one. What remains for each is the screen that uses it.

- [x] **Authenticator** — time-based one-time passwords verified against all eighteen published specification test vectors rather than against their own expectations, plus pairing-URI build and parse. Nothing prints or characterises a secret.
- [x] **Narration** — off by default, injectable, a voice choice per language, strictly one utterance at a time, a superseded line replaced rather than stacked, ordinary narration rate-limited and an error never dropped.
- [x] **Colour engine** — every format both directions with alpha preserved or reported lossy, contrast checked against the reference pairs, out-of-gamut colours flagged before clipping, and the animated rainbow modelled as a sentinel that must never enter a palette of real colours.
- [x] **Unlock ladder** — clears the waiting and never the credential, never refunds the attempt budget, budgeted to three skips an hour because a machine can play it, single-use nonces, and a timed round that cannot be won faster than it lasts. The first two rules are structural: no field exists that could carry a token, and no code path can add budget.
- [x] **Changelog** — parsing, date and text filtering that compose, an invalid pattern reported rather than thrown, a version with nothing recorded surviving as an empty entry, and a commit reference refused unless it is a real identifier.
- [x] **Local version history** — append-only, a restore recorded as a new entry rather than a rewrite, commit messages naming what changed, and credential-shaped values redacted before they reach disk.
- [x] **Offline documentation** — all 82 articles generated into the application with a completeness check that fails the build when the bundled count and the count on disk disagree, plus search, link resolution and broken-link reporting.
- [x] **Media library** — prompts and music on hold, refusing by name before a command is built, verifying content by leading bytes where the format has any, and honest that headerless formats cannot be verified that way.

### Canvases worth building

An assessment rejected most candidates as tables wearing a costume — queues, conferences,
access rules, voicemail routing and feature codes are flat, ordered or one-to-one, and a
graph of one-to-one edges is a two-column table with extra steps. Module dependencies were
downgraded to not buildable: no Asterisk command exposes inter-module dependencies, so it
is a wish rather than a proposal. Two survived on merit.

- [x] **Codec translation paths.** `core show translation` is literally an N×N cost matrix, so the data is already graph-shaped rather than needing graph semantics invented for it. Answers "why is this call transcoding twice" and "what is the cheapest common codec" — questions a matrix past six codecs genuinely cannot. Shipped and driven against a running exchange: 18 translation rows parsed from `core show translation`, 46 codecs, an 18-node 306-edge graph with real costs -- ulaw to alaw at 9,150 microseconds is the figure the matrix actually reports, not an illustration.
- [x] **Endpoint to registration topology.** Endpoint, address-of-record, contact and registration as one chain. Answers "why can't this trunk call out", which today means cross-referencing three screens by hand; a broken link in a chain is what a graph shows and a table hides. Shipped and proved by changing the exchange rather than by reading the code. An empty target draws nothing, which is honest and also indistinguishable from a broken canvas -- so an endpoint and its address-of-record were written through the real plan-and-apply path and the canvas went from nought nodes to two joined by a binding edge, then back to nought when the change was undone through the handle the transaction now returns.
- [ ] Highlight time-conditional edges inside the existing dialplan canvas rather than building a second canvas for them — they are already drawn there.
- [ ] Decide whether the pre-scrub installers published before `899a3c3ecf` should be superseded or removed; their binaries carry private wording and cannot be edited. Every release from `899a3c3ecf` onward is clean.
- ~~Correct the two commit messages that carry private wording (`9beed2f159`, `899a3c3ecf`).~~ **Deliberately not doing this.** It would require rewriting published history and force-pushing, and the owner declined. Recorded here so the gap reads as a decision rather than an oversight, and so a later contributor does not rewrite shared history to close it. Every editable surface was swept instead, and a guard refuses new occurrences.
- [x] Run the built Windows console through the approved headless interaction route and record genuine packaged interaction evidence for WSL discovery.

## Desktop contract features that are wired at one end only

Found by writing a per-feature contract test for all 44 rows on each surface, which is the
first thing in this project to look at every one of them. Each entry below is a module that
exists, is tested in isolation, and that nothing a person can reach ever calls -- the failure
this repository keeps repeating, because it produces no error and no failing test.

- [x] **Reach the export and bulk-action code from the buttons that claim to do it.** Fixed at the two points the compiled interface hands off, both inside App.tsx rather than the generated file: `hostAction()` now recognises the Export button's exact call shape (`export-json`/`subject:'selection'`) and routes it into `bulk('Exported', ...)`; a confirmed bulk-Delete ceremony's exact title/command pair (`Delete ${n} objects` / `delete ${ids.join(' ')}`) is parsed back into its id list (`parseBulkDeleteCeremony`, bulk.ts) and routed into `bulk('Deleted', ...)` instead of sending `delete <ids>` -- not a real Asterisk command -- to the target. A single row's own "Delete <name>" ceremony, gated by a different confirmation flow, is untouched. Both
      modules import cleanly and their planning logic genuinely runs, but the compiled interface
      routes Export and Delete through separate code that never passes `bulk()` the verb needed to
      reach the rich branch. Subtler than never importing it, and invisible for the same reason.
- [x] **Give the desktop a responsive mechanism at all.** Real `@media (max-width: …)`
      breakpoints now live in `console/app/renderer/src/styles.css` (targeting the compiled
      shell's own inline styles, since it has no class or `data-*` hook to target instead) and
      narrow the two fixed nav columns and collapse every multi-column grid to a single column
      below the console's own enforced 920px minimum window width; a matching `min-width: 0`
      fix restores a shrink-behind-content safety net the compiled design's own dead rule never
      actually applied. Verified against the real built app: a genuinely clipped control (the
      "+ New …" filter-row button on every table screen) is now fully on-screen at the enforced
      floor. See `console/docs/platform/responsive-sizing.md` for the one known remaining gap
      (a separate, JS-driven title-bar menu overflow below that floor, unreachable in the
      shipped app) and `console/tests/contracts/responsive-sizing.test.mjs`, which still asserts
      the old absence and needs updating to match.
- [ ] **Anchor notifications in a corner and let them stack.** Two of three fixed 2026-08-26,
      one still open:
      - The toast now renders bottom-RIGHT (`right:24px; bottom:24px` in the design's toast
        block, `design/Asterisk Console M3.dc.html`), not bottom-centre.
      - The Notification centre screen is real: `narratedFire`/`gatedToast` (App.tsx), the two
        chokepoints every `fire()`/`toast()` call already passes through, each record into
        `this.notificationHistory`, and `applyRows` renders that real history as the table's
        rows -- newest first, `Source` honestly reporting `notice`/`toast` rather than inventing
        a subsystem neither primitive tags -- instead of the design's four hardcoded sample
        rows. "Mark all read" reaches a real handler, `onMarkAllNotificationsRead`, through the
        same generic table-add dispatch `onAddServer`/`onAddAclRule`/`onAddApiUser` already use.
      - **Still open: stacking.** `toastOpen`/`toastText` are still single scalar state fields,
        so a second `toast()` while one is showing still replaces it rather than queuing beside
        it. Left open rather than attempted alongside the other two, because the shell's own
        `undoToast` is written against exactly one visible toast: it reverts "the most recent
        commit" whenever the toast on screen is the one that commit raised (`undo-toast.ts`).
        A real stack needs that identity threaded per-toast -- which commit each visible toast's
        own Undo button reverts, not merely which commit was most recent -- which is a real
        state-shape change, not a corner reposition.
- [ ] **Import the six finished modules nothing imports.** Two are genuinely reached now;
      four remain blocked on something real, precisely enough to act on:
      - [x] **Bounded overlays.** `App.tsx` shadows the compiled shell's own `setState`
            (`boundedOverlaySetState`) so every click that opens the context menu -- which sets
            `ctxX`/`ctxY` as a raw, unclamped `${clientX}px`/`${clientY}px` pair inside generated
            `console.tsx`, with no bound against the window anywhere -- gets clamped through
            `computeOverlayPlacement` before the real `setState` ever sees it. `lockX`/`lockY` and
            `regexX`/`regexY` copy `ctxX`/`ctxY` the moment they open, so they inherit the clamp
            for free; `showInfo`'s own click-anchored call site is clamped the same way at its
            existing styling wrapper. No design file touched.
      - [x] **Long-operation progress.** `deployProgressLine` (Deploy & servers' provisioning
            status) is now driven by the module's real weighted state machine instead of a bare
            step count. The actual blocker recorded against this module was correct and sharper
            than "nothing imports it": provisioning is really two fixed step sequences
            (`provisioning.provision(true)` vs `provisioning.provisionFromBaseImage()`, chosen by
            `dispatch.ts` before the first step exists), and one plan cannot describe both without
            `reportProgress` throwing. Resolved with two phase lists
            (`DEPLOY_PHASES`/`DEPLOY_PHASES_FROM_BASE_IMAGE`) and `firstStepPlan`, which reads the
            first real step to pick the one a run actually opened. A 2-second ticker keeps the
            percentage and stall message live through `wsl --import`/the base-image download's
            silent minutes, since those phases carry no intermediate step of their own.
      - [ ] **Context-menu shortcuts.** Every hint shown beside a context-menu item (`⌃L`, `F2`,
            `⌃D`, `⌦`, `⌃W`, `⌃E`, `F1`, `⌃R`, …) is a hardcoded literal in generated `console.tsx`
            with no real global key handler behind it. Confirmed by grep: the one genuine global
            chord this app has is `Ctrl+Shift+F` for the command palette (`App.tsx`'s
            `listenForPaletteChord`) -- nothing else in the renderer, generated or not, ever calls
            `window.addEventListener('keydown', …)`. Making the module's own contract true (the
            displayed shortcut is the one that actually fires) means either registering real
            global handlers for each one -- a genuinely new, context-dependent feature well past
            wiring -- or rewriting the hint literals at their source, which live entirely inside
            `design/Asterisk Console M3.dc.html`'s compiled output, e.g.
            `console/app/renderer/src/generated/console.tsx` around the `ctxItems` builders near
            lines 5578-5710. Neither fits "pure wiring."
      - [ ] **Collapsible filters.** No search bar, filter row or the one statistics panel this
            app renders (the dashboard's stat-card grid, `console/app/renderer/src/generated/console.tsx:315-330`,
            `A(v.stats).map(...)`) has any collapse/expand affordance anywhere in the compiled
            design. The module's toggle button, `aria-expanded` wiring and persisted collapsed
            state have no existing element to attach to without adding one to the design file.
      - [ ] **Forge publishing.** Confirmed by grep: no `control-plane` action, OAuth flow, or
            git-remote-push call site exists anywhere in this app for the module's
            account/owner/fork-vs-copy decision logic to sit in front of. The "History & git"
            screen's `hi_push` ("Mirror to a remote") switch
            (`console/app/renderer/src/generated/console.tsx:3494`) is the one UI surface shaped
            like it, but it is a bare persisted boolean with nothing behind it -- the real
            config-change history transport (`ConfigHistory`, `console/control-plane/dispatch.ts:643`)
            keeps target-side backup handles, not a local git repository to mirror. Wiring the
            module to that switch would make an app-owned control claim an action it cannot
            perform.
      - [x] **Status hub.** 2026-08-26: reached for the one thing it can genuinely do without a
            network transport -- build and validate a report about *this* window's own state,
            locally. The Status hub screen's "Record this session" button
            (`ctl('b_build', ...)`, action `hub-report-build`) reaches `App.tsx`'s
            `onBuildHubSession`, which builds a `SessionReport` from `this.configs` (one lane per
            config screen this run has tried to read, `passed`/`failed` with real evidence, never
            invented), runs it through `validateReport`, and stores the `buildPayload` result in
            `this.hubSessions`; `applyRows` renders that real history as the table's rows,
            replacing the design's three hardcoded sample sessions. Still no HTTP transport
            exists anywhere in the app -- nothing here is ever sent anywhere, and the button and
            `agent-rail.ts`'s `hubNote` both say so plainly. Real remote consumption still needs
            everything the paragraph above described: a configured host, a session key, and an
            `hub.*` control-plane action to carry the validated payload to it.
- [x] **Correct the implementation registry where its notes are now false.** It still records that
      per-element locks have no one-time-code option and no documented context-menu command; both
      shipped. Their real gap is that credentials sit in plain state rather than the operating
      system vault. It also understates appearance, where four of six symbols do write real styles
      and only theme import is dead.
## Controls that announce themselves instead of working

- [x] **Give the missing React keys a real source.** MISDIAGNOSED WHEN THIS WAS WRITTEN, and
      the correction is the useful part. This item claimed a "pinned list" feature was
      rendering children without keys. There is no pinned list. `Pinned` is the throwaway
      subclass name every UI test uses to freeze a render (`class Pinned extends App`), so it
      appeared in several hundred warnings purely as the component React happened to name.
      Reading the warning text and inventing a feature to match it produced a roadmap item for
      a thing that does not exist.
      The real cause, traced with an instrumented copy of react-dom-server (restored
      afterwards and confirmed byte-identical): `title-bar-name.ts` calls
      `cloneElement(node, undefined, rewritten)` to rewrite the compiled title bar's app name
      without touching generated output, and when a rewritten node had more than one child it
      handed the whole array across as a single argument. React cannot tell that from an
      unkeyed `.map()`, so it stopped treating those children as static. The title bar renders
      on every screen, which is why one seam produced warnings everywhere and made the noise
      look like a list problem.

Found by the repository owner watching a drive of the built application and noticing that
a click produced a message and nothing else. Every one below is styled as a live control,
is reachable, and does nothing but raise a notification -- which the project already
forbids: anything presented as operable must perform its labelled action.

- [x] **Build the seven menus in the menu bar.** File, Edit, View, PBX, Agent, Window and Each opens the real context-menu overlay with real items: File and Window operate tab and workspace state, View docks and goes full-screen, PBX carries the six existing quick actions hoisted into one shared constant so the dashboard and the menu cannot drift, Agent lists its seven screens, Help reaches the documentation and history.
      Help are generated by one line that maps each label to a handler raising the text
      "<label> menu". No menu opens, and no item exists behind any of them.
- [x] **Stop claiming actions that never happened.** Five notifications state an outcome The guard scan became real, routed through the same confirmation flow its two sibling panels already used -- it was the only one of three skipping it. The rest became honest: a new branch, an exported bundle and a branch-from-here have no repository or bundle operation anywhere in the control plane; a detail request has no partner channel; and authenticator pairing has a static checkerboard for a code and an unlock that never checks one. Each is dimmed, carries a tooltip naming exactly what is missing, and says it is not built rather than reporting success.
      in the past tense while nothing runs: a branch created from the current commit, a
      bundle written to disk, a guard scan queued, a detail request sent to a named
      partner, and a built-in authenticator paired. A control that does nothing is a
      defect; one that says it succeeded is a false statement to the person reading it.
- [x] **Make the two canvas actions real.** Adding a step and duplicating a node both Both now push through the same mechanism the right-click duplicate already used, so the canvas genuinely gains the node.
      report a change to the canvas that the canvas never receives.
- [x] **Make copy-to-clipboard actually copy.** Two handlers report that a value was Both colour-format handlers now perform the real clipboard write the sibling copy controls were already wired to.
- [x] **Fix the two remaining controls of the same class.** The tab-list copy read `t.label` off `tabs`, an array of plain screen-key strings with no `label` of their own -- fixed (`tabListText`) to resolve each key the way every other tab label in this console already does: `tabNames` first, the destination's compiled title second, the raw key last. The global Undo could not, in general, know what a given toast even was -- most toasts are not a value change at all -- so it now decides honestly (`decideUndo`, undo-toast.ts): when the toast on screen is genuinely the one `setVal()` raised for the most recently recorded control change, Undo reverts that value through the same `setVal()` route the History screen's own "Revert just this option" already uses; for anything else -- a bulk summary, a reroll, a ceremony's own "Running..." -- it says plainly there is nothing recorded here to reverse, rather than claiming "Change reverted" as before. A global Undo on the shared
      notification reverts nothing, and the copy-tab-list item writes the literal text
      `undefined` to the clipboard because it reads a label off the wrong shape.
      copied without writing to the clipboard.
- [x] **Screens naming a configuration file they can never write** -- resolved, and the guard that tracked them stays. Every screen either names a real file it reads, or names a deliberate label and reads through its own view (`agiscripts` cross-checks `dialplan show` against `asterisk.conf`'s `astagidir`). The pin in resource-for-file.test.tsx fired five separate times getting here, twice reporting a list nobody expected, and is kept rather than retired: the last time it nearly emptied, a brand-new instance arrived in the same wave that fixed the previous one.
      records screens carry a compound display string of two file names joined by a
      separator. The transport matches a resource by exact name, so that string resolves to
      nothing and neither screen can write through it. Found while looking for a pattern to
      copy, and deliberately not copied.
- [x] **On the hosted site, the regex builder claimed to be active and filtered nothing.**
      Found by actually driving the published site in a real browser (Edge on an off-screen
      desktop, proven single-target, over the debugging protocol) rather than reading the
      source and assuming: open any of the six search fields wired to the regex builder,
      apply a valid pattern from an EMPTY search box, and the mode status honestly says
      "Regular expression search active" -- while the result list kept showing everything,
      completely unfiltered, until the user also typed a stray character into the plain-text
      field. Root cause was `matchText()`'s own `if(!query)return true` firing before it ever
      consulted the active regex, plus a second, independent copy of the same shortcut inside
      `changelogSearch()` that bypassed `matchText()` entirely. Both are fixed in
      `console/site/app.js`; the fix is a two-line reorder plus a one-line deletion, and it
      repairs all six affected surfaces at once (documentation search, the changelog, local
      notifications, local history, the settings-page search, and the command palette) because
      every one of them already shared the one `matchText()` helper. Guarded by a new,
      behavioural test in `console/site/tests/site.test.mjs` that extracts the real shipped
      `matchText`/`changelogSearch` source and runs it rather than asserting on a string.
- [x] **On the hosted site, exporting local settings gave no on-screen confirmation.** Every
      other export control on the site (destination search results, notifications, the
      changelog) pairs its real `download()` call with a `notify()` toast so the action is
      confirmed somewhere a screen reader or low-vision user can actually perceive it. The
      settings page's own "Export redacted settings" button called `download()` alone --
      the file genuinely downloaded (confirmed by watching Edge's own download surface open),
      it just said nothing about it happening. Now calls `notify('Settings exported', ...)`
      like its siblings; guarded by an anchored source assertion in `site.test.mjs`.


## Accessibility, which is a completion blocker and has never had an entry here

The shared rules treat an accessibility defect as a blocker rather than polish, and the
handoff has called the desktop's accessibility absent for some time. These numbers are
measured, not asserted: they come from the running packaged build, driven headlessly, with
the onboarding wizard dismissed and its dismissal verified before anything was counted.

Baseline, out of **426 rendered elements** on the main surface:

| Measured | Before | After |
| --- | --- | --- |
| Elements carrying any ARIA role | **1** | **9–10** (`banner`, `main`, `navigation`×2, `tablist`, `tab`×N, `tabpanel`, `status`) |
| Semantic landmarks (`nav`, `main`, `aside`, `header`, `footer`, or the matching ARIA role) | **0** | **4** (title-bar banner, primary rail, section list, content area) |
| Elements carrying `aria-label` | **0** | **7** |
| Elements carrying `tabindex` | **0** | **4** on the main surface (more on the docs and history screens, see below) |
| Distinct tag names in the whole body | **6** (`A`, `BUTTON`, `DIV`, `H1`, `P`, `SPAN`) | **12** |

The "after" figures come from `node console/scripts/ui-drive/a11y-probe.mjs <port>` against
the real packaged build (element count grew slightly between runs from unrelated live-state
content, not from anything below). All **96** buttons already carried real text before any
of this, so every one of them already had an accessible name; the gap was always structure.

- [x] **Give the interface landmarks.** The title bar is `role="banner"`, the primary rail
      and the section list are each `role="navigation"` with their own `aria-label`, and the
      content area is `role="main"` with an `aria-label` that tracks the active screen title.
      Done in `design/Asterisk Console M3.dc.html` and recompiled, never in the generated
      renderer. Verified structurally (measured 4 landmarks on the live build, up from 0) and
      functionally not re-tested beyond that, since a landmark's only job is to be found by
      role, which the probe above confirms directly.
- [x] **Give the tab strip the roles it is pretending to have.** `role=tablist`/`tab`/`tabpanel`
      were already present from the prior session; what was missing was proof they actually
      work. Verified live against the packaged build: focusing the first tab and dispatching a
      real `ArrowRight` keydown on the tablist moved both DOM focus and `aria-selected` to the
      next tab, flipped the roving `tabindex` from `0`/`-1` correctly on both tabs, and
      `aria-controls`/`aria-labelledby` already tie `tabpanel-content` back to the active tab.
      Home/End are wired the same way in `tabsKeyDown` (design script, ~line 4716).
- [x] **Make focus reachable and visible.** Two real gaps closed, plus a fresh live-build
      keyboard test.
      - *Visible:* a global `:focus-visible` ring already existed for buttons, links, inputs,
        `role=tab`/`button`/`tabpanel`/`dialog`/`menu`/`[tabindex]` (`app/renderer/src/styles.css`),
        but six inline `outline:none` declarations on plain-chrome text inputs (regex builder,
        PIN, passphrase, unlock passphrase, tab-filter text, rename field) sat at higher CSS
        specificity than that rule and silently defeated it — an inline style always beats an
        external selector without `!important`. Removed all six; verified live that a focused
        row now measures `outline: solid rgb(130,217,165) 2px` via `getComputedStyle`.
      - *Reachable:* swept the whole design file for every `onClick` on a non-native,
        non-`role`-bearing element. Found 15: 8 are backdrop-scrim click-catchers (dismiss an
        open dialog on outside click) that correctly stay out of the tab order, 5 were real
        interactive rows with no keyboard path (the version-history commit list, the in-app
        docs-browser result and suggested-article rows, and the article body's inline internal
        links, in both their paragraph and list-item rendering), and 2 are the dialplan
        canvas's topology-node picker — a draggable 2D diagram, left unfixed here because real
        keyboard graph navigation is its own feature rather than a `tabindex` addition. Fixed
        the 5: added `role="button" tabindex="0"` plus a shared `activateOnEnter` handler
        (`app/renderer/src/App.tsx`) that mirrors a native button's Enter/Space activation.
        Also gave the two context-menu item lists `role="menuitem"` for correctness against
        their `role="menu"` parent. Verified live on the packaged build: focused the docs
        result row for "Operations & releases" and dispatched a real `Enter` keydown; the
        selection highlight moved from the previously-selected row to it, proving the keyboard
        path genuinely selects rather than merely being present in the markup.
      - *Return-to-opener:* already handled generically by `listenForOverlayFocusReturn` in
        `App.tsx` — a `MutationObserver` on `[role="dialog"], [role="menu"]` that remembers the
        triggering element on open and restores it on close. Confirmed by reading the code and
        confirming the appearance editor (`appearOpen`, line ~1777) is itself `role="dialog"`,
        so it is covered by the same mechanism as every other dialog and menu without a
        bespoke case. Not independently re-verified live in this pass; the mechanism predates
        this session and its own logic was read, not re-derived.
      - **Known remaining gap:** the dialplan canvas's draggable topology nodes are not
        keyboard-reachable. Named rather than silently left out.
- [x] **Cover it with a test that reads the built artifact, not the source.** Added
      `console/scripts/ui-drive/a11y-probe.mjs`: dismisses and verifies the onboarding wizard
      exactly as `smoke.mjs` does, measures the same five counts as this table, refuses to
      trust a stale artifact the same way `smoke.mjs` does, and exits non-zero when any count
      drops below a floor set a little under the current measured value (never the exact
      figure — a guard pinned to the exact number breaks on the next unrelated content change
      and gets "fixed" by whoever hits it first). Documented in
      `console/docs/operations/build-and-release.md`. Also fixed a pre-existing, already-broken
      guard found while working in this area: `design-drift.test.mjs`'s binding-count test
      expected `onKeyDown: 2` from a regex (`: fn\(`) that can never match `onKeyDown`, because
      the compiler (`scripts/compile-design.mjs`) never wraps `onKeyDown` in `fn(...)` — so the
      assertion was failing at the base commit before this session touched anything (confirmed
      by stashing this session's changes and re-running the test in isolation). Fixed by
      counting `onKeyDown` separately and updating the expected total to 7, the real count
      after this session's five new `onKeyDown` bindings; verified red-then-green by setting a
      deliberately wrong value, watching it fail, then restoring the correct one.

## Release readiness

- [x] Verify `download-dependencies.bat /s` from a clean user-scoped toolchain cache.
- [x] Verify `build.bat /s` at the merged candidate commit.
- [x] Verify `build-installer.bat /s` produces an unsigned installable Squirrel.Windows set.
- [x] Bundle a complete Ubuntu WSL root filesystem containing Asterisk and every runtime dependency inside the installer.
- [x] Verify the static Pages output includes `console/site/dist/build-manifest.json` and deploys without runtime asset fetches.
- [x] Rework the static Pages home into a modern responsive marketing and documentation surface while preserving all 32 destination identifiers and honest release availability.
- [x] Split the static experience into compact Home, Product, Documentation, Downloads, Status, and Settings routes with shared local assets and anchored article navigation.
- [x] Publish and independently verify the first unique non-draft Material Asterisk release and downloadable assets. Verified at `ding-pbx-console-v0.0.5-r1`: non-draft, non-prerelease, target `50dad7aadbc8c8c3b79ecc844245ea977509daf3`, carrying `Ding-PBX-Console-Setup.exe` (422,853,632 bytes), `ding-pbx-console-0.1.0-full.nupkg` (422,856,987 bytes), `RELEASES`, `SHA256SUMS.txt`, and both line-count evidence files. `RELEASES` and `SHA256SUMS.txt` were downloaded and read back, and the size `RELEASES` records for the full package matches the published asset exactly.
- [x] Add real built-artifact updater evidence for the old `0.1.0` baseline and installed `0.1.84` draft-blocked state, with source commits `745d7425df791646aef4a6972c96dcf279a6004a`, `870be47d6708b32f7fed154abf0ca3779f1fe3bb`, and `b29850dd1ae63553dc6c60ecdedc60adb6707a77`, exact release tags, SHA-256 image records, dimensions, hidden-desktop CDP method, direct installer launch, restart, Later persistence, and draft blocking documented in `console/docs/platform/automatic-updates-evidence.md`.
