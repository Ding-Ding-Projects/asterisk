# UI smoke evidence

The reviewed UI smoke contract is documented in `console/docs/platform/ui-smoke-evidence.md`. It reconciles 940 unique design ids, 940 generated-runtime ids, 696 screen-model ids, 244 dynamic runtime ids, and the prior 335-record subset across 143 explicit surfaces. It derives 17,127 rows as 5,486 real action cases × 3 language modes = 16,458 control rows, plus 669 route and viewport proofs. A completed evidence pass must link the integrated commit, exact before and after captures, version-1 interaction, outcome, privacy, alt-text, pixel-review, and evidence receipts, labelled comparisons, visual diffs, and the byte-preserving promotion index.

Asterisk admin-resource wiring, FreePBX parity, migration successor identities, migration, backup, local Git history, and configurable shared-instructions settings remain pending and are intentionally absent from the implemented feature and evidence totals.

The canonical repository evidence target is `console/docs/evidence/ui-smoke/<integrated-commit>/`. Raw capture bytes remain in the task-owned hidden-desktop run root and are never committed directly. The Pages mapping is `console/site/documentation.html#ui-smoke-evidence-<integrated-commit>`, and the issue mapping is one inline evidence comment per accepted row.
